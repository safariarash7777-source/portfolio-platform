# نمونه‌های طراحی — بازبینی‌نشده

> ⚠️ **هیچ‌کدام از این‌ها تصویب نشده‌اند.** پیشنهادِ بصری‌اند، نه تصمیم.
> وضعیتشان در [`COMMAND-CENTER.md`](../../COMMAND-CENTER.md) §۱۳ دنبال می‌شود.
> نیازهای پشتِ آن‌ها (`R-001`…`R-012`) در همان سند §۱۲ است — همه `PROPOSED`.

## بازبینیِ نسخهٔ ۱٫۱ — چه چیزی اصلاح شد

نسخهٔ اولِ این نمونه چند عددی را نشان می‌داد که **دادهٔ واقعی پشتشان نیست**.
برای یک نمونهٔ طراحی این خطرناک‌تر از حالتِ خالی است: بازبین فرض می‌کند آن
بخش شدنی است و رویش تصمیم می‌گیرد.

| اصلاح | چرا | شاهد |
|---|---|---|
| «۹۰ روزِ اخیر» → **«۳۷ روزِ معاملاتیِ موجود · از ۱۴۰۵/۰۴/۲۷»** | تاریخچهٔ NAV روی Production ۳۷ روزِ معاملاتی است؛ نمودارِ ۹۰ روزه ساختنی نیست | `symbol_history` با `raw ? 'nav_toman'`: ۳۷ روزِ معاملاتیِ متمایز، پوششِ ۱۰۰٪ همان بازه |
| «باندِ متعارفِ ۹۰ روزِ گذشته» → **باندِ همان ۳۷ روز** + توضیحِ «چرا سه ماه نه» | همان دلیل — و همان قاعده‌ای که `bubbleWindow` در #120 اعمال می‌کند | `lib/core/fundBubble.ts` |
| «خالص دارایی ۱۲٬۴۰۰ م.ت» → **«—» در همهٔ ردیف‌ها** | `assetB` در فیدِ زنده برای **هر ۳۲۶ صندوق `null`** است | اسنپ‌شاتِ `ir_market_snapshots` |
| «۱۹ صندوق طلا» → **«۳۵ صندوق طلا»** | اسنپ‌شاتِ زنده ۳۵ صندوقِ «کالایی مبتنی بر طلا» دارد، هر ۳۵ دارای NAV | همان اسنپ‌شات |
| کارتِ ترکیبِ دارایی: «منبع نداریم» → **«منبع پیدا شد؛ انتخابِ مسیر مانده»** | «صورت وضعیت پورتفوی صندوق» ماهانه در کدال هست: ۷۴۸ اطلاعیه، ۴۹۷ صندوق، پوششِ ۲۲۶ از ۳۲۶ | `codal_feed` · و رله از قبل از `excel.codal.ir` دانلود می‌کند |
| شناسهٔ تصمیم → **`D-026`** | `D-030` تکراری بود و بازنشسته شد | `DECISION-LOG.md` · `MARKET-CORE-AUDIT.md` §۴.۱ |
| حالتِ «ناقص» نماد: برچسبِ **«نمادِ فرضی»** + توضیح | نمونه ادعا می‌کرد فملی گزارشِ ن-۳۰ ندارد؛ فملیِ واقعی **۳۶ گزارشِ ن-۳۰** و **۱۸ گزارشِ ن-۱۰** دارد | `codal_reports` |
| «بازار ۵٪ بالاتر … می‌خرد» → «واحدِ صندوق … معامله می‌شود» | نزدیک‌شدن به واژگانِ ممنوعِ قاعدهٔ `L1` بی‌دلیل بود | `lib/core/vocab.test.ts` |

> اعدادِ باقی‌مانده همچنان **نمایشی**‌اند و بنرِ بالای صفحه همین را می‌گوید.
> فرقِ این نسخه: هیچ عددِ نمایشی، قابلیتی را وعده نمی‌دهد که داده‌اش وجود ندارد.

---

## فایلِ نمونه

`market-core-prototype.html` — یک فایلِ مستقل و بدونِ وابستگی. در مرورگر باز کنید.
توکن‌های رنگ عیناً از `app/globals.css` آمده‌اند تا نمونه با محصول یکی باشد.

با نوارِ بالای صفحه می‌توان عوض کرد: **صفحه** (خانهٔ عضو / صندوق / نماد) ·
**اندازه** (دسکتاپ / موبایل) · **حالتِ داده** (کامل / ناقص / کهنه / خطا) · **تم** (روشن / تیره).

لینکِ مستقیم به یک حالت هم کار می‌کند:

```
market-core-prototype.html#fund/mobile/stale/dark
market-core-prototype.html#symbol/partial
```

## اسکرین‌شات‌ها

| فایل | صفحه | حالت |
|---|---|---|
| `01-home-desktop-full.png` | خانهٔ عضو | دسکتاپ · دادهٔ کامل · روشن |
| `02-fund-desktop-full.png` | صندوق | دسکتاپ · دادهٔ کامل · روشن |
| `03-symbol-desktop-full.png` | نماد | دسکتاپ · دادهٔ کامل · روشن |
| `04-home-mobile-error.png` | خانهٔ عضو | موبایل · **خطا** |
| `05-fund-mobile-stale-dark.png` | صندوق | موبایل · **کهنه** · تیره |
| `06-symbol-desktop-partial.png` | نماد | دسکتاپ · **ناقص** |
| `07-fund-desktop-partial-dark.png` | صندوق | دسکتاپ · **ناقص** · تیره |

## چطور بازتولید کنم

```bash
/opt/pw-browsers/chromium --headless --no-sandbox --disable-gpu --hide-scrollbars \
  --window-size=1440,1300 --virtual-time-budget=2500 \
  --screenshot=out.png \
  "file://$PWD/docs/assets/design-review/market-core-prototype.html#fund"
```

> **دامِ شناخته‌شده:** Chromiumِ headless کمینه‌عرضِ viewport ≈ ۴۸۵px دارد.
> برای عکسِ موبایل، `--window-size=800,…` بدهید و در hash کلیدواژهٔ `mobile` را بگذارید
> تا از قابِ شبیه‌سازِ ۳۹۰px استفاده شود. عکس‌گرفتن با `--window-size=390` تصویرِ
> بریده و جابه‌جا می‌دهد — این یک artifactِ عکس‌برداری است، نه باگِ چیدمان.
