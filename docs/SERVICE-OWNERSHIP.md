# مالکیتِ سرویس‌ها (Service Ownership)

> نامِ انسانِ ناشناخته جعل نشده. جایی که مالکِ مشخص نبود، `OWNER_UNASSIGNED`.
> مالکِ حسابِ همهٔ زیرساخت‌ها: **آرش صفری** (GitHub: `safariarash7777-source`) — تأییدشده via `get_me`.
>
> **این سند منبعِ حقیقتِ مالکیت است.** خانه‌های `OWNER_UNASSIGNED` به‌عنوان بلاکرِ
> **`B-018`** در [`COMMAND-CENTER.md`](./COMMAND-CENTER.md) §۳ و تصمیمِ **`D-004`** در
> [`DECISION-LOG.md`](./DECISION-LOG.md) پیگیری می‌شوند. تغییرِ مالکیت باید هر سه سند را
> با هم به‌روز کند.

| حوزه | مالکِ حساب/تصمیم | مجریِ عملیاتی | Blocker / Decision | یادداشت |
|---|---|---|---|---|
| **Portfolio (Vercel)** | آرش صفری | آرش + ایجنت‌های Claude | `B-009`، `B-010` · `D-005`، `D-006` | حسابِ Vercel متعلق به آرش؛ راستی‌آزماییِ مستقیمِ حساب در این سشن انجام نشد |
| **Supabase (DB/Auth)** | آرش صفری | آرش + ایجنت‌ها | `B-001` · `D-001` | سازمان `foglnmjnfppaiqdbbgof`؛ تنها پروژه `uooeygybrniptzdxuzhj` |
| **Relay (Liara)** | آرش صفری | آرش (کلیدها/دیپلوی) | — | app `arsadata`؛ توکنِ Liara دستِ آرش |
| **Mini App (Manus Legacy)** | آرش صفری | آرش | `D-003` | نسخهٔ زنده؛ برنامهٔ مهاجرت به Docker/Coolify (ADR-001) |
| **Mini App (هدف Docker/Coolify/VPS)** | آرش صفری | `OWNER_UNASSIGNED` (VPS هنوز نیست) | `B-005`، `B-006`، `B-007`، `B-008`، `B-018` · `D-003`، `D-004` | بر مبنای PR #2 (merge شده؛ **مستقر نشده**) |
| **Scheduler** | Vercel Cron (منبعِ اصلی) + Relay (مستقل) | آرش | `B-009`، `B-015`، `B-016` · `D-005` | ADR-002 |
| **Deployment** | آرش صفری | آرش + ایجنت‌ها (با اجازهٔ صریح) | — | push/PR/deploy فقط با okِ آرش |
| **Database migrations** | آرش صفری | ایجنت (پیشنهاد) → آرش (تأیید) | `B-001`، `B-004` · `D-001`، `D-002` | اجرا فقط با تأیید؛ MIGRATION-LEDGER |
| **Domain / DNS** | آرش صفری | `OWNER_UNASSIGNED` (جزئیاتِ DNS راستی‌آزمایی‌نشده) | `B-018` · `D-004` | خارج از دامنهٔ P1-001 |

> اصلِ حاکم: **یک مالکِ واحد در هر لحظه** برای هر منبعِ درحالِ ویرایش (به‌ویژه وقتی دو ایجنت هم‌زمان کار می‌کنند — ONBOARDING §۱۲).
