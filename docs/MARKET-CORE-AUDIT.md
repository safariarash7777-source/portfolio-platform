# هستهٔ تحلیل بازار — وضعیتِ واقعی، ممیزیِ داده، و برنامهٔ اجرا

> **نقشِ این سند:** پاسخ به سه سؤالِ اولِ مأموریتِ «تثبیت و توسعهٔ هستهٔ تحلیل بازار»:
> «الان واقعاً چه داریم»، «چه دادهای مصرف می‌کنیم و چقدرش خوانده می‌شود»، و
> «با چه ترتیبی جلو می‌رویم».
>
> **این سند وضعیتِ کلِ پلتفرم را نمی‌گوید** — آن کارِ [`COMMAND-CENTER.md`](./COMMAND-CENTER.md)
> است. اینجا فقط **هستهٔ تحلیل بازار** است: صندوق، نماد، رادار، میزِ تحلیل، پنلِ عضو.
>
> **هیچ ادعایی بدون شاهد.** هر ردیف یا مسیرِ فایل دارد، یا نتیجهٔ یک اندازه‌گیریِ
> فقط‌خواندنیِ Production، یا شمارهٔ تست.

**تاریخِ اندازه‌گیری:** ۲۰۲۶-۰۹-۰۶ · **Production ref:** `uooeygybrniptzdxuzhj`
· **مبنای کد:** `origin/main` روی `1d0759f`
· **روشِ اندازه‌گیری:** پرس‌وجوهای فقط‌خواندنی (`SELECT`/`information_schema`/`pg_class`).
هیچ `INSERT`/`UPDATE`/`DELETE`/DDL اجرا نشد.

---

## ۰. سه خبرِ اصلی، پیش از جزئیات

۱. **لولهٔ داده کار می‌کند، ولی یکنواخت تازه نیست.** جدیدترین ردیفِ یک جدول
   دربارهٔ سلامتِ تک‌تکِ نمادها چیزی نمی‌گوید؛ پس به تفکیک اندازه گرفته شد
   (بخش ۱.۰). خلاصه: از ۱٬۱۳۱ نمادِ `symbol_history`، **۱٬۰۵۷ (۹۳.۵٪) امروز
   داده دارند**، ۹ تا یک روز عقب‌اند، ۴۰ تا بین ۲ تا ۳۶۵ روز عقب‌اند و
   **۶ نماد بیش از یک سال است هیچ ردیفی نگرفته‌اند** (قدیمی‌ترین: `2018-10-29`).

۲. **هیچ‌کدام از migrationهای `phase20` تا `phase25` روی Production اجرا نشده‌اند.**
   اندازه‌گیریِ زنده: صفر جدولِ `intel_*`، `cron_runs` غایب، `leads` غایب،
   `entitlement_durations` غایب، `geopolitical_events` غایب، تابعِ
   `finalize_paid_access` در فهرستِ ۳۳ تابعِ `public` **نیست**.
   یعنی **میزِ تحلیلِ خصوصی و مسیرِ پرداخت→دسترسی کدشان نوشته شده ولی روی
   Production وجودِ خارجی ندارند.** این دقیقاً همان چیزی است که
   [`MIGRATION-LEDGER.md`](./MIGRATION-LEDGER.md) هم می‌گوید — سند و واقعیت منطبق‌اند.

۳. **دو حسابِ واقعی در کلِ سامانه وجود دارد.** آمارِ تجمیعی (بدونِ هیچ فیلدِ
   شخصی): `auth.users` = **۲** · `profiles` = **۲** · حسابِ بدونِ پروفایل = **۰**
   · پروفایلِ بدونِ حساب = **۰** · حسابِ ساخته‌شده در ۹۰ روزِ اخیر = **۰**
   · `entitlements` = **۰** · `telegram_links` = **۰**. یعنی جدولِ `profiles`
   ناقص نیست — **اصلاً کاربری وجود ندارد.** در برابر ~۶۰۰–۷۰۰ عضوِ واقعی.
   حتی اگر فردا هر پنج قابلیتِ هسته سبز شود، **مخاطبی پشتِ لاگین نیست.** مسیرِ «عضوِ موجود → حسابِ پلتفرم»
   در هیچ کجای کد وجود ندارد و در هیچ سندی هم به‌عنوان کارِ باز ثبت نشده.
   این بزرگ‌ترین شکافِ کسب‌وکار است، نه یک شکافِ فنی.

---

## ۱.۰ سلامتِ لوله — به تفکیکِ مجموعه‌داده، نماد و گروهِ دارایی

> **چرا این بخش اضافه شد.** نسخهٔ اول این سند از «جدیدترین ردیفِ هر جدول» نتیجه
> گرفت که لوله سالم است. آن استدلال غلط است: یک نمادِ زنده کافی است تا
> `max(trade_date)` امروز شود، در حالی که صدها نماد مرده باشند. اینجا به تفکیک
> اندازه گرفته شده.

### تازگی به تفکیکِ نماد — `symbol_history`

| فاصلهٔ آخرین ردیف تا آخرین روزِ معاملاتی | تعداد نماد | سهم |
|---|---:|---:|
| همان روز (`2026-09-06`) | ۱٬۰۵۷ | ۹۳.۵٪ |
| ۱ روز | ۹ | ۰.۸٪ |
| ۲ تا ۷ روز | ۱۹ | ۱.۷٪ |
| ۸ تا ۳۰ روز | ۲۱ | ۱.۹٪ |
| ۳۱ تا ۳۶۵ روز | ۱۹ | ۱.۷٪ |
| **بیش از یک سال** | **۶** | ۰.۵٪ |
| **جمع** | **۱٬۱۳۱** | |

قدیمی‌ترین «آخرین ردیف» در کلِ جدول: `2018-10-29`. این ۶ نماد احتمالاً متوقف یا
حذف‌شده‌اند — ولی **هیچ‌جای کد یا داده این را نمی‌گوید**؛ از نظرِ خواننده‌ها با
یک نمادِ زنده تفاوتی ندارند. این خودش یک نقص است (بخش ۳، ردیفِ `G`).

### ابزارها به تفکیکِ گروهِ دارایی — اسنپ‌شاتِ زندهٔ `ir_market_snapshots`

| گروه | در اسنپ‌شات | دارای داده | فاقدِ داده | ذخیره می‌شود؟ |
|---|---:|---:|---:|---|
| اختیارِ معامله | ۱٬۳۱۹ | ۱٬۳۱۹ | ۰ | فقط لحظه‌ای (بدون تاریخچه) |
| سهام | ۷۶۳ | ۷۶۳ | ۰ | ✅ `symbol_history` |
| صندوق | ۳۲۶ | **۳۰۳** (NAV) | **۲۳ بدونِ NAV** | ✅ `symbol_history` |
| ارز | ۲۸ | ۲۸ | ۰ | ✅ `ir_market_history` |
| رمزارز | ۱۹ | ۱۹ | ۰ | فقط لحظه‌ای |
| گواهیِ سپردهٔ کالایی | ۱۱ | ۱۱ | ۰ | ⚠️ `ime_snapshots` **۰ ردیف** |
| طلا و سکه | ۹ | ۹ | ۰ | ✅ `ir_market_history` |
| **کامودیتیِ جهانی** | **۰** | **۰** | — | ⚠️ فید هست، خروجی **صفر** |

دو یافتهٔ تازه که در نسخهٔ اول نبود:

- **`commodities` در اسنپ‌شات صفر عضو دارد.** `relay/commodity.mjs` هر ۳۰ دقیقه
  صدا زده می‌شود (کلیدِ رایگانِ جدا) و هیچ ردیفی تولید نمی‌کند. **علتش بررسی
  نشده** — ممکن است کلید، ممکن است تغییرِ قراردادِ منبع. سهمیه مصرف می‌شود.
- **گواهیِ سپرده جمع می‌شود ولی ننشسته:** ۱۱ ردیف در اسنپ‌شات، `ime_snapshots`
  صفر ردیف. یعنی push یا شکسته یا هرگز وصل نشده.

### تازگی به تفکیکِ مجموعه‌داده

| مجموعه‌داده | آخرین رکورد | تازه؟ |
|---|---|---|
| `symbol_history` | `2026-09-06` (روزِ معاملاتی) | ✅ ولی نه برای همهٔ نمادها — جدولِ بالا |
| `codal_reports` | `2026-09-06 11:10` | ✅ |
| `ir_market_history` | `2026-09-06 11:27` | ✅ |
| `fx_rates` | `2026-09-06` | ✅ |
| `market_breadth` | `1405-06-15` | ✅ |
| `index_history` | `1405-06-15` | ✅ |
| `ime_snapshots` | — | ❌ صفر ردیف |
| `macro_first_print` / `macro_revisions` | — | ❌ صفر ردیف |

> ⚠️ **دربارهٔ روشِ شمارش.** نسخهٔ اول این سند برای چند جدول از شمارشِ **تخمینی**
> (`reltuples`) استفاده کرده بود. تخمین با شمارشِ واقعی فرق می‌کند: مثلاً
> `risk_assessments` تخمین **۲** بود و شمارشِ دقیق **۵** است. همهٔ اعدادِ این
> نسخه با `count(*)` گرفته شده‌اند، نه تخمین.

---

## ۱. وضعیتِ پنج قابلیتِ هسته

برچسب‌ها: **موجود و آزموده** · **موجود ولی ناقص** · **فقط مستند** · **غایب** · **مسدود**
(«مسدود» = کد هست و تست دارد، ولی پیش‌نیازِ بیرونی — معمولاً migrationِ اجرانشده — مانع است.)

### ۱.۱ تحلیل صندوق

| جزء | وضعیت | شاهد |
|---|---|---|
| قیمتِ لحظه‌ای و تابلو | **موجود و آزموده** | `lib/market-ir.ts` (`IrStockRow`) · `app/market/funds/page.tsx` · `components/market/FundsFullBoard.tsx` |
| NAV صدور/ابطال | **موجود ولی ناقص** | `relay/server.mjs:297,386` (`Tsetmc/Nav.php`) · `lib/core/navHistory.ts`. زنده: ۳۱۱ صندوق دستِ‌کم یک مشاهدهٔ NAV دارند؛ ۳۰۳ در اسنپ‌شاتِ امروز؛ **۲۳ صندوقِ اسنپ‌شات اصلاً NAV ندارند** |
| حباب (اختلافِ قیمت و NAV) | **موجود ولی ناقص** | `bubblePercent` در `lib/market-ir.ts:52` — محاسبهٔ قطعیِ رله. **فقط لحظه‌ای؛ سریِ تاریخیِ حباب هنوز خواننده ندارد** |
| بازدهٔ دوره‌ای (۱ه/۱م/۳م) | **موجود و آزموده** | `lib/core/fundReturns.ts` + `fundReturns.test.ts` · مصرف در `app/market/funds/page.tsx` |
| خالص دارایی (اندازهٔ صندوق) | **غایب** (تصحیحِ نسخهٔ اول) | `assetB` در `IrRow:26` تعریف شده ولی در اسنپ‌شاتِ زنده **برای هر ۳۲۶ صندوق `null` است**. فیلد هست، داده نیست |
| نقدشوندگی | **موجود ولی ناقص** | `volume`/`value` در تابلو هست؛ هیچ ماژولِ سنجشِ نقدشوندگی (عمقِ دفتر، شکافِ عرضه/تقاضا، روزهای بی‌معامله) وجود ندارد |
| مقایسهٔ هم‌گروه | **موجود ولی ناقص** | فیلترِ `type` در `FundsFullBoard`؛ **هیچ موتورِ مقایسهٔ هم‌گروه** (رتبه در گروه، فاصله از میانهٔ گروه) نیست |
| **ترکیبِ دارایی صندوق** | **غایب — و منبعِ داده هم نداریم** | کاتالوگِ مجازِ BrsApi (`references/brsapi.md`) برای صندوق فقط `AllSymbols` و `Nav` دارد. **هیچ endpointِ ترکیبِ دارایی در کاتالوگ نیست** |
| **تغییرِ ترکیبِ دارایی** | **غایب** | پیامدِ مستقیمِ ردیفِ بالا |

> ⚠️ **گلوگاهِ شمارهٔ ۱ مأموریت.** «ترکیبِ دارایی و تغییرِ ترکیب» اولین قلمِ
> فهرستِ شماست و **تنها قلمی است که با منابعِ فعلی اصلاً شدنی نیست.** طبق قاعدهٔ
> `S1` اسکیل، منبعِ تازه فقط با مستنداتِ رسمی + تأییدِ آرش اضافه می‌شود و اسکرپ
> ممنوع است. تصمیمِ باز: `D-030` (بخش ۴).

> **عمقِ NAV — عددِ دقیق و معنایش.** «۳۷ روز» در نسخهٔ اول مبهم بود. اندازه‌گیریِ
> دقیق: بازهٔ `2026-07-18` تا `2026-09-06` **۵۰ روزِ تقویمی** است و در همین بازه
> `symbol_history` **۳۷ روزِ معاملاتیِ متمایز** دارد. تعدادِ روزهایی که دستِ‌کم یک
> مشاهدهٔ NAV دارند هم **۳۷** است، و مجموعهٔ اختلاف **تهی** است — یعنی
> **NAV هیچ روزِ معاملاتی‌ای را جا نینداخته؛ پوشش ۱۰۰٪ است.**
>
> پوششِ هر صندوق جداگانه: از ۳۱۱ صندوق، میانگین **۳۵.۱** روزِ دارای *هم قیمت و هم
> NAV*، کمینه **۹**، بیشینه **۳۷**؛ **۲۹۹ صندوق ≥ ۳۰ روز** دارند و فقط **۱ صندوق**
> زیرِ ۱۰ روز. و هر **۱۰٬۹۰۹** ردیفِ دارای NAV، `close` هم دارد — یعنی حباب روی
> ۱۰۰٪ مشاهده‌ها محاسبه‌شدنی است.
>
> ⚠️ نتیجهٔ عملی: **حبابِ جاری و سریِ ~۳۷ روزه ساختنی است. مقایسهٔ فصلی یا
> میانگینِ ۶ماههٔ حباب ساختنی نیست** و تا دی‌ماه هم نخواهد بود مگر بک‌فیل شود.

### ۱.۲ تحلیل نماد و بنیادی

| جزء | وضعیت | شاهد |
|---|---|---|
| کارتِ بنیادیِ نماد | **موجود و آزموده** | `lib/core/fundamentalCard.ts` + `fundamentalCard.test.ts` · `components/symbol/SymbolFundamentalCard.tsx` |
| صورتِ سود و زیان (درآمد، سودِ ناخالص/عملیاتی/خالص) | **موجود و آزموده** | `lib/fundamental/supabase.ts`. زنده: **۲٬۹۹۳ گزارشِ ن-۱۰ پارس‌شده روی ۴۳۹ نماد**، هر سه قلم در ۱۰۰٪ آن‌ها موجود |
| حاشیهٔ سود | **موجود و آزموده** | مشتق از همان سه قلم در `lib/core/quarterly.ts` + `quarterly.test.ts` |
| فروشِ فصلی (تفاضلِ زنجیره‌ای) | **موجود و آزموده** | `lib/core/quarterly.ts` — قاعدهٔ `D5`: حلقهٔ غایب = فصلِ `null` · `components/symbol/QuarterlyCharts.tsx` |
| فروشِ ماهانه (ن-۳۰) | **موجود و آزموده** | زنده: **۴٬۷۷۹ گزارش روی ۳۱۵ نماد** · `lib/fundamental/n30.ts` + تست |
| **مقدار / نرخ / ترکیبِ محصول** | **موجود و آزموده** | `lib/core/monthlyReport.ts:204-277` (سریِ نرخ، ترکیبِ محصول، تولید در برابر فروش) + `monthlyReport.test.ts`. زنده: `sales_qty` در ۴٬۷۶۴ و `sales_rate` در ۴٬۷۵۹ گزارش موجود است. **کامنتِ «برای فازِ بعد» در `lib/fundamental/types.ts:64` کهنه است و باید اصلاح شود** |
| صورتِ وضعیتِ مالی (ترازنامه) | **غایب** | پارسرِ ن-۱۰ فقط اقلامِ سود و زیان را استخراج می‌کند؛ دارایی/بدهی/حقوقِ صاحبانِ سهام در `data` نیست ⇒ نسبت‌های اهرمی و بازده حقوق صاحبان سهام محاسبه‌پذیر نیستند |
| جریانِ وجهِ نقد | **غایب** | همان دلیل |
| مجموعهٔ گزارش‌ها روی صفحهٔ نماد | **موجود و آزموده** | `components/symbol/CodalReportsTab.tsx` · `codal_feed` با ۹٬۲۴۸ اطلاعیه |

### ۱.۳ رادارِ تغییر

| جزء | وضعیت | شاهد |
|---|---|---|
| گزارشِ تازهٔ کدال | **موجود و آزموده** | `codal_feed` (۹٬۲۴۸ ردیف) · `app/codal/page.tsx` · `app/api/codal-list/route.ts` |
| تشخیصِ اصلاحیه | **موجود ولی ناقص** | تقدمِ «حسابرسی‌شده، سپس بزرگ‌ترین id» در `lib/fundamental/supabase.ts:22-24` اعمال می‌شود، ولی **اصلاحیه هرگز به‌عنوان یک رویداد اعلام نمی‌شود** — بی‌صدا جایگزینِ نسخهٔ قبلی می‌شود |
| تغییرِ معنادارِ عملکرد | **موجود و آزموده** (فقط ادمین) | `lib/core/marketRadar.ts` + `marketRadar.test.ts` — چهار گلوگاه: جریانِ پولِ حقیقی، تغییرِ عملیاتیِ ن-۳۰، واگراییِ تابلو، حجمِ نسبی · `app/(protected)/admin/radar/page.tsx` |
| اقلامِ نیازمندِ بررسی (صفِ کار) | **غایب** | هیچ جدول/صف/حالتِ «دیده شد» وجود ندارد؛ رادار هر بار از صفر محاسبه می‌شود |
| «چه چیزی از آخرین بازدیدِ من تغییر کرده» | **غایب** | نه نشانهٔ زمانِ آخرین بازدید، نه دلتا، نه اعلان |

> رادار **امروز یک گزارشِ لحظه‌ای است، نه یک رادار.** رادار باید بگوید «از دیروز
> چه چیزی عوض شد»؛ این نسخه می‌گوید «امروز وضع چنین است». فاصله‌اش یک جدولِ
> وضعیت است، نه یک موتورِ تازه.

### ۱.۴ میزِ تحلیلِ خصوصی

| جزء | وضعیت | شاهد |
|---|---|---|
| قراردادِ داده و تجمیع | **موجود و آزموده** | `lib/desk/contracts.ts` (چهار حالتِ `ready`/`stale`/`empty`/`unavailable`) + چهار فایلِ تست در `lib/desk/` |
| گردشِ کارِ شاهد → ادعا → انتشار | **موجود و آزموده** | `lib/intelligence/workflow.ts` + `workflow.test.ts` + `workflow.integration.test.ts` |
| سناریو و شرطِ تغییرِ نظر | **موجود و آزموده** (در sql) | `sql/phase20_intelligence_model.sql` (۱۵ جدول) · `sql/phase22_manual_intelligence_workflow.sql` |
| ورودیِ ژئوپلیتیک | **موجود و آزموده** (در sql) | `sql/phase25_geopolitical_intake.sql` + `lib/intelligence/geopolitical.test.ts` |
| **همهٔ موارد بالا روی Production** | **مسدود** | صفر جدولِ `intel_*` روی Production. `phase20`/`phase21`/`phase22`/`phase25` هیچ‌کدام اجرا نشده‌اند. صفحهٔ `/admin/desk` هر منبع را `unavailable` گزارش می‌کند — که رفتارِ **درست** است، ولی یعنی میز کار نمی‌کند |

### ۱.۵ پنلِ عضو

| جزء | وضعیت | شاهد |
|---|---|---|
| تحلیلِ منتشرشده | **موجود ولی ناقص** | `app/analyses/page.tsx` · `lib/track/analyses.ts` + تست. شمارشِ دقیق: `signals` **۱**، `weekly_outlooks` **۰** ⇒ صفحه عملاً خالی است |
| واچ‌لیست | **موجود ولی ناقص** | `lib/core/watchlist.ts` · `app/(protected)/terminal/watchlist/page.tsx`. زنده: `watchlist_items` **۰ ردیف** |
| پروفایلِ ریسک | **موجود و آزموده** | `app/(protected)/dashboard/page.tsx` · `risk_assessments` **۵ ردیف** روی **۲ کاربرِ متمایز** (شمارشِ دقیق؛ تخمینِ `reltuples` عددِ ۲ می‌داد) |
| پرسش و پاسخ | **غایب** | هیچ جدول، هیچ مسیر، هیچ کامپوننت |
| **دسترسیِ پولی (پرداخت → عضویت)** | **مسدود** | `payments` ۰ ردیف · `entitlements` ۰ ردیف · `finalize_paid_access` روی Production **وجود ندارد** · PR #113 هنوز باز و Draft است |
| **حسابِ عضو** | **غایب (شکافِ کسب‌وکار)** | `auth.users` **۲** · `profiles` **۲** · بدونِ هیچ ناهماهنگی · **۰ حسابِ تازه در ۹۰ روز** · در برابر ~۶۰۰–۷۰۰ عضوِ واقعی |

---

## ۲. ممیزیِ مصرفِ داده — چه می‌گیریم، چه نگه می‌داریم، چقدرش خوانده می‌شود

### ۲.۱ سرویس‌های بالادست (کاتالوگِ مجاز — `references/brsapi.md`)

| Endpoint | چند وقت یک‌بار | مصرف‌کننده | خوانده می‌شود؟ |
|---|---|---|---|
| `Market/Gold_Currency.php` | هر ۵ دقیقه | `relay/server.mjs:94` | ✅ صفحهٔ اول، `/market` |
| `Tsetmc/AllSymbols.php?type=1` | هر ۵ دقیقه | `relay/server.mjs:158` | ✅ تابلوی سهام و صندوق |
| `Tsetmc/Nav.php` | ساعتی + دورِ تکمیلی | `relay/server.mjs:297,386` | ✅ حباب و NAV |
| `Tsetmc/Index.php?type=1` | جابِ EOD | `relay/server.mjs:507` | ✅ `index_history` |
| `Tsetmc/Candlestick.php?type=2` | بک‌فیلِ سهمیه‌بندی‌شده | `relay/candle-backfill.mjs:214` | ✅ `symbol_history` |
| `Tsetmc/Symbol.php` | on-demand، کشِ ۳ دقیقه | `relay/symbol-detail.mjs` | ✅ صفحهٔ نماد |
| `Codal/Announcement.php` | هر ۳ ساعت | `relay/codal.mjs:435,565` | ✅ `codal_feed` + `codal_reports` |
| `Tsetmc/Option.php` | هر ۵ دقیقه | `relay/options.mjs` | ✅ `/market/options` |
| `IME/Certificate.php` | هر ۳۰ دقیقه | `relay/ime.mjs` | ⚠️ `ime_snapshots` **۰ ردیف** — جمع می‌شود ولی جایی ننشسته |
| `IME/Physical.php` | روزی یک‌بار | `relay/ime.mjs` | ⚠️ همان |
| `Market/Commodity.php` | هر ۳۰ دقیقه (کلیدِ رایگانِ جدا) | `relay/commodity.mjs` | ✅ کامودیتیِ جهانی |

سهمیه در مبدأ بودجه‌بندی شده (`candle-backfill.mjs:31` سقفِ ساعتی و روزانه؛
`symbol-detail.mjs` شمارندهٔ روزانه با ریستِ تهران). قاعدهٔ `Q2` رعایت شده.

### ۲.۲ ذخیره‌سازی — جایی که واقعاً مشکل داریم

اندازه‌گیریِ زندهٔ ۲۰۲۶-۰۹-۰۶:

| جدول | حجم | ردیف | خواننده |
|---|---|---|---|
| `symbol_history` | **۷۴۹ MB** | ۲٬۱۰۶٬۴۳۷ | ✅ رادار، بازده، نمودار، NAV |
| `ir_market_history` | **۴۲۰ MB** | ۹٬۲۷۸ | ⚠️ فقط بخشِ `gold`/`currency` |
| `codal_reports` | ۱۵ MB | ۷٬۷۸۴ | ✅ کلِ مسیرِ بنیادی |
| `codal_feed` | ۶.۹ MB | ۹٬۲۴۸ | ✅ `/codal` |
| بقیه (۳۶ جدول) | < ۳ MB مجموع | — | — |
| **کلِ دیتابیس** | **۱٬۲۱۲ MB** | | |

**نشتیِ فعال — نرخِ رشد، با مبنای اندازه‌گیریِ صریح.**

| کمیت | ۲۰۲۶-۰۸-۲۸ (ممیزیِ PR #119) | ۲۰۲۶-۰۹-۰۶ (این اندازه‌گیری) |
|---|---|---|
| حجمِ کل | ۳۴۸ MB (`pg_size_pretty`، گردشده) | **۴۴۰٬۶۵۵٬۸۷۲ بایت = ۴۲۰.۲ MiB** (بایتِ خام) |
| ردیف | ۷٬۷۹۸ | **۹٬۲۹۰** |
| بازهٔ داده | ۴۵ روز | ۵۵ روز (`2026-07-13` → `2026-09-06`) |

- **مبنا:** `pg_total_relation_size` (heap + TOAST + ایندکس). TOAST تنها
  **۴۳۲ MB از ۴۴۱ MB** است — یعنی ۹۸٪ حجم، خودِ `payload` است.
- **نرخِ اندازه‌گیری‌شده در این ۹ روز:** ۴۲۰.۲ − ۳۴۸ = **۷۲.۲ MiB** ⇒
  **۸.۰ MiB در روز**. (نسخهٔ اولِ این سند «۷۲ MB در ۹ روز» را اشتباه به
  ۷.۳ MB/روز تبدیل کرده بود؛ تقسیمِ درست ۸.۰ است.)
- **گردشدن:** عددِ ۳۴۸ از `pg_size_pretty` می‌آید و تا ±۰.۵ MiB گرد شده، پس
  نرخ **۸.۰ ± ۰.۰۶ MiB/روز** است — گردشدن اینجا بی‌اثر است.
- **تفاوت با برآوردِ قبلی:** آن سند **۷.۷ MB/روز** گفته بود (۳۴۸ ÷ ۴۵ = میانگینِ
  کلِ بازه). عددِ ۸.۰ میانگینِ **۹ روزِ اخیر** است. اختلافِ ۴٪ یعنی نرخ کمی
  **بالا رفته**، نه اینکه یکی از دو اندازه‌گیری غلط باشد — دو کمیتِ متفاوت‌اند.
- ردیف/روز: (۹٬۲۹۰ − ۷٬۷۹۸) ÷ ۹ = **۱۶۵.۸**؛ برآوردِ قبلی ≈۱۷۳. سازگار.
- **سهمِ بخش‌های بی‌خواننده، بازاندازه‌گیری‌شده:**
  `stocks` ۲۷۱ MB + `funds` ۱۲۴ MB = **۳۹۵ MB از ۴۰۱ MB کلِ payload = ۹۸.۵٪**
  (در برابر `gold` ۳.۲ MB و `currency` ۲.۷ MB).

**نشتی هنوز باز است.** ولی یک تصحیحِ مهم نسبت به تصویرِ «رشدِ بی‌پایان»:
`relay/server.mjs:667` یک `HISTORY_RETENTION_DAYS = 180` و `pruneHistory` روزانه
دارد. جدول تازه ۵۵ روزه است، پس این prune هنوز هیچ ردیفی حذف نکرده. یعنی رشد
**بی‌پایان نیست، ولی سقفش ≈۱.۴ GB است** — بیش از کلِ دیتابیسِ امروز.

> **این کارِ تمام‌شده‌ای است که فقط اجرا نشده.** PR #119 هم اندازه‌گیری، هم
> برنامهٔ هفت‌گیتیِ آرشیو، هم کدِ `lib/ops/retention.ts` و هم سوئیچِ
> `IR_HISTORY_SECTIONS` در رله را دارد. **دوباره ساختنش اشتباه است.**
> کارِ باقی‌مانده استقرارِ رله است، نه کدنویسی.

### ۲.۳ چیزهایی که جمع می‌کنیم و هیچ‌کس نمی‌خواند

| داده | چرا مسئله است |
|---|---|
| `ir_market_history` بخش‌های `stocks`/`funds` | ۹۸٪ حجمِ آن جدول؛ صفر خواننده؛ ۷.۳ MB/روز |
| `ime_snapshots` | رله می‌کشد، جدول **۰ ردیف** است — یا push شکسته یا هرگز وصل نشده. **بررسی‌نشده** |
| `macro_first_print` / `macro_revisions` | ۰ ردیف؛ اسکیما هست، هیچ ورودی‌ای وصل نیست |

---

## ۳. برنامهٔ اجرا — ترتیبی که با آن جلو می‌رویم

مبنای ترتیب: **اول چیزی که خون‌ریزی می‌کند، بعد چیزی که بدونِ آن بقیه بی‌معنی
است، بعد قابلیتِ تازه.**

| # | کار | چرا این ترتیب | پیش‌نیاز | وضعیت |
|---|---|---|---|---|
| **A** | بستنِ نشتیِ `ir_market_history` (گامِ ۱ سندِ PR #119 — فقط سوئیچِ رله) | تنها کارِ فهرست که **هر روزِ تأخیر هزینهٔ برگشت‌ناپذیر دارد**. ریسک صفر، دیتابیس دست نمی‌خورد | استقرارِ رله روی لیارا | **منتظرِ اجرا** (کد آماده در PR #119) |
| **B** | مسیرِ «عضوِ موجود → حسابِ پلتفرم» | ۶۰۰–۷۰۰ عضو داریم و ۲ پروفایل. بدونِ این، پنلِ عضو و پرداخت هر دو بی‌مخاطب‌اند | تصمیمِ آرش دربارهٔ روشِ ورود | **تصمیمِ باز `D-031`** |
| **C** | بکاپِ اثبات‌شده روی Production | دروازهٔ اجباریِ هر migration؛ بدونِ آن D و E قفل‌اند | اجرای `RUNBOOK-backup-windows.md` روی ویندوزِ آرش | **مسدود — دستِ مالک** |
| **D** | اجرای `phase20`→`phase21`→`phase22`→`phase23`→`phase24`→`phase25` | میزِ تحلیل و پرداخت هر دو پشتِ همین‌اند. ترتیب اجباری است (`MIGRATION-LEDGER` بند ۴۵-۵۱) | C | **مسدود پشتِ C** |
| **E** | merge شدنِ PR #113 (پرداخت → دسترسی) | بدونِ D بی‌اثر است | D + بازبینیِ مستقلِ Codex | **Draft، ۱۴ کامیت عقب از `main`** |
| **F** | رادارِ تغییر: افزودنِ حالتِ «دیده‌شده» و دلتا | تبدیلِ گزارشِ لحظه‌ای به رادارِ واقعی. مستقل از C/D — روی جدولِ تازهٔ خودش | — | **آماده برای شروع** |
| **G** | تکمیلِ مسیرِ صندوق: سریِ تاریخیِ حباب، نقدشوندگی، مقایسهٔ هم‌گروه | داده‌اش موجود است؛ فقط موتور و نما لازم دارد | — | **آماده برای شروع** |
| **H** | ترکیبِ داراییِ صندوق | **بدونِ منبعِ داده شدنی نیست** | تصمیمِ `D-030` | **مسدود — تصمیمِ مالک** |
| **I** | ترازنامه و جریانِ نقد در پارسرِ ن-۱۰ | نسبت‌های اهرمی و بازده حقوق صاحبان سهام را باز می‌کند | — | **آماده برای شروع** |
| **J** | پرسش و پاسخِ عضو | آخرین قلم؛ پیش‌نیازش B است | B | — |

**کارهایی که همین حالا و بدونِ هیچ تصمیمِ باز می‌توان شروع کرد: F، G، I.**
هر سه فقط از دادهٔ موجودِ Production می‌خوانند، هیچ migrationی روی Production
لازم ندارند و هیچ‌کدام به بکاپ گره نخورده‌اند.

### ۳.۱ قراردادِ داده — قواعدی که این هسته زیرِ آن‌ها نوشته می‌شود

این‌ها تازه نیستند؛ از `CLAUDE.md` و اسکیلِ `iran-market-data` می‌آیند و اینجا
فقط برای هستهٔ تحلیل صریح شده‌اند:

1. **دادهٔ غایب `null` است.** هیچ درون‌یابی، هیچ نزدیک‌ترین‌همسایه، هیچ صفرِ
   جایگزین. الگوی مرجع: `lib/core/fundReturns.ts` که با نبودِ نقطهٔ مبنا
   بازده را گزارش **نمی‌کند**.
2. **چهار حالت، نه دو حالت.** هر منبع باید `ready`/`stale`/`empty`/`unavailable`
   را از هم جدا کند. «خالی است» با «نمی‌بینم» یکی نیست. مرجع: `lib/desk/contracts.ts`.
3. **واحدها یک‌بار در مرزِ ingest تبدیل می‌شوند** و کامنت می‌گیرند
   (`symbol_history` ریال · ن-۱۰/ن-۳۰ میلیون ریال · NAV تومان).
4. **جدول‌های تاریخی append-only** و بدونِ upsertِ مخرب.
5. **هیچ محاسبه‌ای بیرون از `lib/core/`** — نما فقط نمایش می‌دهد.
6. **زیرنمادِ رقم‌دار در مبدأ فیلتر می‌شود** (`Z1`).
7. **واژگانِ ممنوع** (سیگنال/خرید/فروش/توصیه/پیشنهاد) در هیچ خروجی — تضمین
   با `lib/core/vocab.test.ts`.
8. **هر قابلیتِ تازه پیش از UI تست دارد** — `npm run test:core` سبز.

---

## ۴. تصمیم‌هایی که فقط آرش می‌تواند بگیرد

| شناسه | تصمیم | چرا مسدودکننده است |
|---|---|---|
| **`D-030`** | کدام مسیر برای **ترکیبِ داراییِ صندوق** — بخشِ ۴.۱ | ✅ **دیگر مبهم نیست.** داده روی کدال هست و ۶۹٪ صندوق‌های زنده را پوشش می‌دهد؛ مسیرِ دانلودش هم از قبل ساخته و مجاز است. تصمیمِ لازم فقط انتخابِ بینِ سه گزینهٔ بخشِ ۴.۱ است |
| **`D-031`** | تأییدِ طرحِ ورودِ اعضا + تحویلِ فایل — بخشِ ۴.۲ | اجزای برگشت‌پذیرش ساخته و با دادهٔ مصنوعی آزموده شد. آنچه مانده: تأییدِ طرح و تحویلِ فایل با ستون‌های مشخص‌شده، **بیرون از چت** |
| **`D-032`** | آیا `ime_snapshots` و `macro_*` نگه داشته شوند یا خاموش | خلافِ قاعدهٔ `Q3` جمع می‌شوند؛ اگر مصرف‌کننده‌ای در راه نیست، جمع‌آوری باید بایستد |
| `C` (اجرایی) | اجرای بکاپ روی ویندوز | دروازهٔ همهٔ migrationها |

---

## ۴.۱ `D-030` — منبعِ ترکیبِ داراییِ صندوق: از «تصمیمِ مبهم» به گزینهٔ قابلِ‌تصمیم

> **تصحیحِ مهم نسبت به نسخهٔ اول این سند.** آنجا نوشته بودم «منبعِ داده نداریم».
> این **غلط بود**. دادهٔ ترکیبِ دارایی از همان مسیری که الان هزینه‌اش را
> می‌دهیم به ما می‌رسد — فقط پارس نمی‌شود.

### آنچه واقعاً هست (اندازه‌گیریِ زندهٔ ۲۰۲۶-۰۹-۰۶)

صندوق‌ها هر ماه گزارشی به نامِ **«صورت وضعیت پورتفوی صندوق سرمایه‌گذاری»** در
کدال منتشر می‌کنند. این دقیقاً همان ترکیبِ دارایی است. در `codal_feed` ما:

| کمیت | مقدار |
|---|---|
| اطلاعیهٔ «صورت وضعیت پورتفوی» | **۷۴۸** |
| نمادهای صندوقِ متمایز | **۴۹۷** |
| از ۳۲۶ صندوقِ زندهٔ اسنپ‌شات، پوشش‌داده‌شده | **۲۲۶ (۶۹٪)** |
| بازهٔ انتشار | ۱۴۰۴/۰۳/۳۱ تا ۱۴۰۵/۱۰/۳۰ |
| دارای **لینکِ اطلاعیه** | ۷۴۸ (۱۰۰٪) |
| دارای **لینکِ اکسل یا PDF** | **۱ از ۷۴۸** |

### چرا این یک تصمیمِ مجوزی **نیست**

قاعدهٔ `S1` اسکیل می‌گوید: «منبعِ دادهٔ مجاز فقط BrsApi (**+ فایل‌های عمومی
کدال/TSETMC**)». یعنی فایلِ عمومیِ کدال از قبل در دایرهٔ مجاز است — و رله
**همین حالا هم این کار را می‌کند**: `relay/codal.mjs:443` تابعِ
`downloadExcelHtml` دارد و `relay/codal.mjs:646` با یک allowlistِ سخت‌گیرانه
(`^https://excel\.codal\.ir/`) فقط همان دامنه را می‌پذیرد. گزارش‌های ن-۱۰ و
ن-۳۰ که امروز پارس می‌شوند از همین مسیر می‌آیند.

پس مسئله **مجوز نیست، دو حفرهٔ مشخصِ فنی است**:

1. **فیدِ BrsApi برای این نوعِ گزارش `link_excel` نمی‌دهد** (۱ از ۷۴۸). و موتور
   در `relay/codal-engine.mjs:209` صریحاً می‌گوید
   `if (!kind || !a.link || !a.link_excel) continue;` — پس این اطلاعیه‌ها
   بی‌صدا رد می‌شوند.
2. `relay/codal.mjs` فقط `category=1` (صورت‌های مالی) و `category=3` (فعالیتِ
   ماهانه) را می‌خواهد. این گزارش در دستهٔ **«سایر»** است.

### سه گزینه، با آنچه دربارهٔ هرکدام **می‌دانیم** و **نمی‌دانیم**

| گزینه | پوشش | تناوب | تأخیر | تاریخچه | هزینهٔ معلوم | شرایطِ استفاده | اتصال |
|---|---|---|---|---|---|---|---|
| **الف) پرسش از BrsApi** — آیا `Codal/Announcement.php` می‌تواند برای این نوع هم لینکِ پیوست بدهد | ۶۹٪ امروز | ماهانه | نامعلوم | از ۱۴۰۴/۰۳ | **صفر** (اشتراکِ فعلی) | همان قراردادِ فعلی | صفر تغییرِ معماری |
| **ب) دانلودِ فایلِ عمومیِ کدال از `excel.codal.ir`** با همان allowlistِ موجود | همان ۶۹٪ | ماهانه | ساعت | همان | صفر ریالی · هزینهٔ سهمیه و مهندسی | **در دایرهٔ `S1` هست** — ولی باید در `references/brsapi.md` مستند و تأیید شود | مسیر و پارسر موجود است |
| **ج) ورودِ دستیِ فایلِ مجاز** با ثبتِ منبع و تاریخ | هر چه آرش بگذارد | دستی | روز/هفته | فقط از لحظهٔ شروع | زمانِ آرش | بی‌ابهام | جدولِ ساده لازم دارد |

**پیشنهاد: اول (الف)، چون رایگان است و اگر جواب بدهد بقیه لازم نیست.** اگر نه،
(ب) — که فنیاً آماده است و فقط تأییدِ صریح و مستندسازی می‌خواهد. (ج) به‌عنوان
پشتیبان می‌ماند، نه راهِ اصلی؛ ۴۹۷ صندوق ماهانه دستی شدنی نیست.

> ⚠️ **آنچه ادعا نمی‌کنم.** `fund.fipiran.ir` از این محیط **قابلِ دسترسی نبود**
> (`ENOTFOUND`)، پس دربارهٔ وجود، مجوز یا شرایطِ استفادهٔ API آن **هیچ ادعایی
> ندارم**. وجودِ داده روی یک سایت، مجوزِ API یا بازنشر نیست.

---

## ۴.۲ `D-031` — طرحِ ورودِ اعضای موجود

اجزای برگشت‌پذیرش ساخته و روی Postgresِ یک‌بارمصرف آزموده شد
(`sql/phase27_member_import.sql` + ۴۰ تست، **همه با دادهٔ مصنوعی**).
**هیچ فهرستِ واقعی وارد نشد، هیچ حساب یا دسترسیِ واقعی ساخته نشد، هیچ دعوتی
فرستاده نشد.**

قاعدهٔ حاکم: **حضور در کانالِ تلگرام مدرکِ خرید نیست.** ستونِ `evidence` روی
هر دسته اجباری است، پس هیچ ردیفی بدونِ گفتنِ «این آدم بر چه اساسی حق دارد»
وارد نمی‌شود.

| جزء | چطور تأمین شد |
|---|---|
| فهرستِ مجاز با پیش‌نمایش | ردیف با وضعیتِ `pending` وارد می‌شود؛ **ورود هیچ دسترسی‌ای نمی‌دهد** تا تأییدِ صریحِ دسته |
| گزارشِ خطا | وضعیت‌های `unmatched`/`rejected` **علتِ نوشته‌شده لازم دارند** (قید دیتابیس) |
| تطبیق با شناسهٔ تأییدشده | `matched` بدونِ `matched_user_id` با قید رد می‌شود |
| دورهٔ واقعی و تاریخِ پایان | `access_from`/`access_until` از خودِ فهرست؛ `access_until > access_from` قید است — **پیش‌فرضِ سیستمی وجود ندارد** |
| جلوگیری از ثبتِ تکراری | ایندکسِ یکتا روی (دسته، نوعِ تماس، مقدار) |
| جلوگیری از اعطای دوباره | ایندکسِ **جزئیِ** یکتا روی `user_id` تا وقتی `revoked_at IS NULL` — پس یک نفر در دو فهرستِ متفاوت هم دسترسیِ دوم نمی‌گیرد |
| رسیدگی به تطبیق‌نیافته‌ها | حالتِ معتبرِ `unmatched` با علت — نه خطا، نه حذفِ خاموش |
| سابقهٔ اعطا | `member_grants` با زمانِ اعطا |
| لغوِ موردی | `revoke_member_batch(batch, reason)` — علتِ ≥۱۰ نویسه؛ **هیچ ردیفی پاک نمی‌شود**، فقط `revoked_at` می‌خورد؛ پس از لغو اعطای تازه مجاز است |
| حریمِ خصوصی | شناسهٔ تماس دادهٔ شخصی است: `anon` هیچ، کاربرِ عادی هیچ ردیفِ فهرست، و فقط **اعطای خودش** را می‌بیند |

### چه فایل و چه ستون‌هایی بعداً از مالک لازم است

یک فایل `.csv` با `UTF-8` و دقیقاً این ستون‌ها:

| ستون | اجباری | توضیح |
|---|---|---|
| `contact_kind` | ✅ | `email` یا `phone` |
| `contact_value` | ✅ | نشانی یا شماره — همان چیزی که عضو با آن ثبت‌نام می‌کند |
| `access_from` | ✅ | تاریخِ شروعِ حق (میلادی `YYYY-MM-DD`) |
| `access_until` | ✅ | تاریخِ **واقعیِ** پایان — نه «سه ماه از امروز» |
| `evidence` | ✅ (یک‌بار برای کلِ فایل) | این فهرست چه چیزی را ثابت می‌کند |

**فایل را در چت نفرست.** مسیرِ تحویل باید جدا تعیین شود؛ این فایل دادهٔ شخصیِ
۶۰۰–۷۰۰ نفر است.

---

## ۴.۳ توقفِ رشدِ دادهٔ بی‌مصرف — بررسیِ سازگاریِ PR #119

**کدِ #119 دوباره ساخته نشد.** آنچه انجام شد بررسیِ سازگاریِ آن با نسخهٔ
امروزِ رله و مصرف‌کننده‌هاست:

| بررسی | نتیجه |
|---|---|
| آیا وصلهٔ #119 روی `main`ِ امروز اعمال می‌شود؟ | ✅ `git apply --check` سبز |
| آیا `relay/server.mjs` روی `main` بعد از انشعابِ #119 تغییر کرده؟ | ❌ هیچ کامیتی — `merge-base` دقیقاً `1d0759f` یعنی HEADِ فعلی |
| تنها مصرف‌کنندهٔ `ir_market_history` کیست؟ | `lib/core/trend.ts:96` — و **تنها آن** (جست‌وجوی مستند روی `lib/`، `app/`، `components/`) |
| چه بخش‌هایی می‌خواهد؟ | `lib/core/trend.ts:89` → `section: "in.(gold,currency)"`؛ هدف‌ها `gold/IR_GOLD_18K` و `currency/USD` |
| آیا پیش‌فرضِ `IR_HISTORY_SECTIONS=gold,currency` چیزی را از دست می‌دهد؟ | ❌ **هیچ‌چیز.** دقیقاً همان دو بخشی است که تنها خواننده می‌خواهد |

**«توقفِ نوشتن» با «حذفِ تاریخچه» یکی نیست** و باید جدا بماند:

| | توقفِ نوشتن (گامِ ۱) | حذفِ تاریخچه (گامِ ۲) |
|---|---|---|
| چه می‌کند | رله دیگر `stocks`/`funds` نمی‌نویسد | ردیف‌های موجود پاک و فضا پس داده می‌شود |
| امروز آزاد می‌کند | **۰ MB** | ≈۳۹۵ MB |
| از رشد جلو می‌گیرد | **۸.۰ MiB/روز** | — |
| دیتابیس لمس می‌شود؟ | **نه** | بله (`DELETE` + `VACUUM FULL`) |
| ریسک | استقرارِ رله | قفلِ `ACCESS EXCLUSIVE` + نیازِ آرشیو |
| پیش‌نیاز | فقط استقرارِ لیارا | **هفت گیتِ آرشیوِ سندِ #119** |

### برنامهٔ آمادهٔ استقرار — فقط گامِ ۱

1. **تنظیم:** روی رلهٔ لیارا `IR_HISTORY_SECTIONS` را **تنظیم نکن** (پیش‌فرضِ
   `gold,currency` همان است). فقط اگر خواستی رفتارِ قبلی برگردد:
   `IR_HISTORY_SECTIONS=gold,currency,funds,stocks`.
2. **شاهدِ قبل:** `SELECT section, count(*) FROM ir_market_history GROUP BY 1`
   و `pg_total_relation_size('public.ir_market_history')` — یادداشت شود.
3. **استقرار** رله.
4. **تست:** `GET /debug` → `historySections.written` باید `["gold","currency"]`
   باشد و `missing` خالی.
5. **شاهدِ بعد (پس از ۲۴ ساعت):** شمارشِ `stocks`/`funds` **نباید افزایش
   یافته باشد**؛ `gold`/`currency` باید افزایش یافته باشند. اگر دومی رشد
   نکرد، رله داده نمی‌دهد و مسئله جای دیگری است.
6. **Rollback:** تنظیمِ env بالا + ری‌استارتِ رله. دیتابیس دست‌نخورده است، پس
   بازگشت بی‌هزینه است.

**هیچ استقرار و هیچ پاک‌سازیِ Production در این جلسه انجام نشد.**

---

## ۴.۴ آمادگیِ فعال‌سازی — وابستگیِ واقعی، نه شمارهٔ phase

### وابستگی‌ها، از خودِ SQL درآمده

| migration | واقعاً به چه چیزی نیاز دارد | ترتیب |
|---|---|---|
| `phase20_intelligence_model` | `auth.users`, `profiles` | پایهٔ زنجیرهٔ هوشمندی |
| `phase21_cron_runs` | مستقل | هر جا |
| `phase22_manual_intelligence_workflow` | **جدول‌های `phase20`** — تنها یالِ واقعیِ سطحِ جدول | حتماً بعد از `phase20` |
| `phase23_grant_hardening` | کلِ اسکیمای `public` را جارو می‌کند | **حتماً آخر** |
| `phase24_payment_entitlement` | `payments`, `entitlements` (هر دو از قبل روی Production) | پس از `phase23` |
| `phase25_geopolitical_intake` | مستقل | هر جا، پیش از `phase23` |
| **`phase26_change_radar`** (این جلسه) | `auth.users`, `profiles`, `is_admin()` — **هر سه از قبل روی Production هستند** | **مستقل از ۲۰ تا ۲۵** |
| **`phase27_member_import`** (این جلسه) | همان سه | **مستقل از ۲۰ تا ۲۵** |

> ⚠️ **شمارهٔ phase ترتیب نیست.** تحلیلِ ساده‌انگارانه یک چرخهٔ کاذبِ
> `phase20 ↔ phase22` می‌سازد چون هر دو یک تابع را `CREATE OR REPLACE` می‌کنند —
> ولی **بازتعریف پیش‌نیاز نیست**.
>
> **نتیجهٔ عملی:** `phase26` و `phase27` پشتِ زنجیرهٔ مسدودِ ۲۰–۲۵ **نیستند**.
> هر پیش‌نیازشان امروز روی Production هست. ولی همچنان پشتِ **دروازهٔ بکاپ**اند.

### دروازهٔ بکاپ — آیا الزامِ ویندوز از دستورِ صریح آمده؟

**نه.** بررسیِ مستند:

- `scripts/backup-production.ps1` برای ویندوز است، ولی `scripts/backup-production.sh`
  هم وجود دارد و **همان کار را روی هر میزبانِ یونیکسی با Docker** می‌کند.
- الزامِ واقعی «ویندوز» نیست؛ **«یک اپراتورِ انسانی که رمزِ دیتابیس دستش
  است، روی ماشینی با Docker»** است. ویندوز صرفاً ماشینِ آرش است.
- علتِ ریشه‌ای در خودِ اسکریپت نوشته شده: سازمان روی پلنِ `free` است و
  Supabase بکاپِ خودکار نمی‌گیرد.
- چرا من نمی‌توانم اجرا کنم: این کانتینر TCPِ مستقیم به دیتابیس ندارد، Docker
  daemon ندارد، و — تعیین‌کننده‌تر — **رمز باید دستِ اپراتور بماند و هرگز وارد
  چت نشود**.

**قاعده دور زده نمی‌شود.** فقط منشأش دقیق شد.

### معیارِ بازیابیِ موفق — «فایل هست» کافی نیست

معیار از قبل در `scripts/backup-production.sh` تعریف شده و همان می‌ماند:

1. بازگردانی در **یک تراکنش** با `ON_ERROR_STOP=1`؛ معیارِ موفقیت **کدِ خروجیِ
   `psql`** است، نه grep روی لاگ.
2. مقصد یک **استکِ Supabaseِ محلیِ ایزوله** است، نه Postgresِ ساده — وگرنه
   اسکیماهای `auth`/`storage` اصلاً آزموده نمی‌شوند.
3. مقایسهٔ **دوطرفه** با `scripts/backup/inventory.sql` + `compare.mjs`:
   شمارشِ **دقیقِ** ردیف‌های هر جدول (نه `reltuples`) و اثرِ انگشتِ ساختاری.
4. `compare.mjs` فقط با **صفر اختلاف در هر دو جهت** کدِ ۰ می‌دهد
   (`compare.mjs:129`)؛ هر مورد غایب، اضافه یا با تعریفِ متفاوت ⇒ کدِ ۱.

---

## ۴.۵ شواهدِ ادعاهای «غایب»

> ادعای «در کلِ کد وجود ندارد» فقط با جست‌وجوی مستند مجاز است. اینها اجرا شدند
> (روی کلِ مخزن، به‌جز `node_modules`) و نتیجه‌شان اینجاست:

| ادعا | الگوی جست‌وجو | نتیجه |
|---|---|---|
| پرسش و پاسخِ عضو **غایب** | `qna\|q_and_a\|question\|پرسش\|answer_\|faq_thread\|ask_arash` روی `*.ts,*.tsx,*.sql,*.mjs` | **هیچ پیاده‌سازی‌ای نیست.** تنها برخوردها واژهٔ `question` به‌عنوانِ برچسبِ UI در `DeskBoard.tsx` و `QuarterlyCharts.tsx` است (پرسشِ راهنمای هر پنل) و پرسشنامهٔ ریسک — که موضوعِ دیگری است |
| صفِ «اقلامِ نیازمندِ بررسی» و حالتِ «دیده‌شده» **غایب** | `reviewed_at\|seen_at\|acknowledg\|dismiss\|review_queue\|radar_state\|last_seen` روی `*.ts,*.tsx,*.sql` | **صفر برخورد.** نه ستون، نه جدول، نه کامپوننت |
| ترازنامه و جریانِ وجهِ نقد **غایب** | `total_assets\|total_liabilit\|equity\|balance_sheet\|cash_flow\|ترازنامه\|جریان وجه` روی `lib/` و `relay/` | **صفر برخوردِ مرتبط.** تنها برخوردها `equity_ir` است — یک عضوِ enumِ طبقهٔ دارایی در `lib/intelligence/contracts.ts`، بی‌ارتباط با صورت‌های مالی |

---

## ۵. آنچه این سند **نمی‌گوید**

- ادعایی دربارهٔ درونیاتِ ETFباز/بورس‌ویو/انیگما ندارد. هیچ‌کدام بررسی نشده‌اند
  و اشتراکِ شخصی مجوزِ استخراج نیست.
- «کد نوشته شد» را با «روی Production کار می‌کند» یکی نمی‌گیرد. ستونِ وضعیت
  دقیقاً همین تفاوت را نگه می‌دارد.
- عددِ عملکردِ آینده، تخمینِ بازده، یا وعده‌ای دربارهٔ زمان‌بندیِ وبینار ندارد.
