# بسته‌های کاری و گیت‌ها

> `P2-INTELLIGENCE-OPERATING-DESIGN-001` · ترتیب **الزامی** است: هر بسته
> فرضِ بستهٔ قبلی است. اجرای بستهٔ ۴ پیش از ۱ یعنی ساختن روی زمینِ نامرئی.

## ترتیبِ اولویت

```
۱. حقیقت و سلامتِ داده        → WP-01, WP-02
۲. ثبتِ منبع و زمان           → WP-03, WP-04
۳. سبدِ مرجع و اثرِ رخداد      → WP-05, WP-06
۴. میزِ قابلِ استفادهٔ روزانه   → WP-07
۵. تمرینِ خصوصیِ ۱۰روزه       → WP-08
۶. تجربهٔ انتخابیِ اعضا و عموم  → WP-09
۷. نخستین Agentِ کمکی         → WP-10
```

---

## WP-01 · اندازه‌گیریِ وضعیتِ زنده

| قلم | مقدار |
|---|---|
| **هدف** | جایگزینیِ هر `UNVERIFIED` با یک عددِ اندازه‌گیری‌شده |
| **مسئله** | طراحی روی فرض ساخته می‌شود چون هیچ‌کس نمی‌داند رله بالاست، جدول‌ها هستند یا cronها اجرا می‌شوند |
| **سیستم‌ها** | `<relay>/debug` · `/api/market?diag=1` · Supabase (فقط خواندن) |
| **وابستگی** | دسترسیِ خواندن به محیطِ زنده |
| **خارج از دامنه** | هر نوشتن، هر migration |
| **ریسک** | پایین — فقط خواندن |
| **پذیرش** | جدولی با یک ردیف برای هر جدول: وجود دارد؟ آخرین `captured_at`؟ تعدادِ ردیف؟ و پاسخِ قطعی به F-11 (آیا جدول‌های IME هستند) |
| **تست** | خروجیِ دو ابزارِ تشخیصیِ موجود، ثبت‌شده در `findings.md` |
| **Rollback** | لازم نیست |
| **مالکِ تصمیم** | مهندسی |

## WP-02 · بستنِ شکافِ تولیدکننده/مصرف‌کننده

| قلم | مقدار |
|---|---|
| **هدف** | هیچ تولیدکننده‌ای به مقصدِ ناموجود ننویسد؛ هیچ جدولی بی‌مصرف پر نشود |
| **مسئله** | F-11: `relay/ime.mjs` به دو جدولی می‌نویسد که دفتر می‌گوید وجود ندارند. F-04: سه جدولِ بدونِ نویسنده و خواننده |
| **سیستم‌ها** | `relay/ime.mjs` · `sql/phase19_ime_tables.sql` · دفترِ migration |
| **وابستگی** | `WP-01` |
| **خارج از دامنه** | **حذفِ هیچ جدول یا داده‌ای** |
| **ریسک** | متوسط — اگر جدول‌ها واقعاً هستند، توقفِ نوشتن دادهٔ واقعی را از دست می‌دهد |
| **پذیرش** | برای هر یک از ۵ جدولِ F-04 یک حکمِ صریح: «مصرف‌کننده ساخته شد» یا «نوشتن متوقف شد» یا «مالک نگه‌داشتن را تأیید کرد» |
| **تست** | اسکریپتی که هر دو الگوی دسترسی را می‌شمارد و روی هر جدولِ بی‌مصرفِ تازه قرمز می‌شود |
| **Rollback** | برگرداندنِ کامیت؛ هیچ داده‌ای حذف نشده |
| **مالکِ تصمیم** | آرش (نگه‌داشتن یا نداشتنِ IME) |

## WP-03 · سیاستِ نگه‌داری برای هر جدولِ تاریخچه‌ای

| قلم | مقدار |
|---|---|
| **هدف** | هر جدول یا سیاستِ هرس دارد یا دلیلِ مکتوبِ نداشتنش |
| **مسئله** | F-03: فقط `ir_market_history` هرس می‌شود (۱۸۰ روز). بقیه هرگز پرسیده نشده‌اند |
| **سیستم‌ها** | `relay/server.mjs` · `docs/` |
| **وابستگی** | `WP-01` (حجمِ واقعی لازم است) |
| **خارج از دامنه** | حذفِ دادهٔ موجود · جدول‌های ممیزی (`codal_reports`, `signals`, `cron_runs`) که **باید** تغییرناپذیر بمانند |
| **ریسک** | بالا اگر اشتباه اجرا شود — بستهٔ بعدی فقط **سیاست** می‌نویسد، اجرا جدا است |
| **پذیرش** | جدولِ «جدول → قرارداد → دوره → دلیل» برای هر ۵۳ جدول |
| **تست** | مرور توسطِ آرش؛ بدونِ اجرای خودکار |
| **Rollback** | سند است؛ Rollback ندارد |
| **مالکِ تصمیم** | آرش |

## WP-04 · آستانه‌های تازگی از روی تناوبِ واقعی

| قلم | مقدار |
|---|---|
| **هدف** | هر شاخصِ تازگی به عددی در `relay/server.mjs` وصل باشد |
| **مسئله** | آستانه‌های امروز از حدس آمده‌اند؛ بعضی «موقت و محافظه‌کارانه» علامت خورده‌اند |
| **سیستم‌ها** | رجیستریِ منابع (سندِ همراه) · هر شاخصِ سلامت |
| **وابستگی** | `WP-01` |
| **خارج از دامنه** | تغییرِ خودِ رله |
| **ریسک** | پایین |
| **پذیرش** | هر آستانه فیلدِ `basis` دارد که به فایل و خط ارجاع می‌دهد؛ آستانهٔ بی‌شاهد وجود ندارد |
| **تست** | تستی که هر `basis` را با ثابتِ واقعیِ کد تطبیق دهد |
| **Rollback** | برگرداندنِ کامیت |
| **مالکِ تصمیم** | مهندسی |

## WP-05 · تصمیمِ migration

| قلم | مقدار |
|---|---|
| **هدف** | تصمیمِ صریح دربارهٔ `phase20/21/22/23` روی Production |
| **مسئله** | F-10 — کلِ مدلِ هوشمندی، دفترِ cron و جدولِ تمرین وجود ندارند. **این تنها گیتی است که ۵ بستهٔ بعدی را قفل کرده** |
| **سیستم‌ها** | Supabase Production |
| **وابستگی** | `WP-01` |
| **خارج از دامنه** | **در این PR هیچ SQL اجرا نمی‌شود** |
| **ریسک** | بالا — تغییرِ شِمای Production |
| **پذیرش** | تصمیمِ مکتوبِ آرش: اجرا شود / نشود / با تغییر. اگر اجرا: چک‌لیستِ پیش‌پرواز شاملِ گرنت‌های `service_role` (درسِ `phase23`) |
| **تست** | راستی‌آزماییِ staging از قبل انجام شده و در دفتر ثبت است |
| **Rollback** | هر فایل باید بخشِ down داشته باشد؛ اگر ندارد، پیش‌نیازِ اجراست |
| **مالکِ تصمیم** | **آرش** |

## WP-06 · نگاشتِ ابزارِ سبدِ مرجع

| قلم | مقدار |
|---|---|
| **هدف** | تبدیلِ ۷۰/۱۵/۱۵ به ابزارِ مشخص |
| **مسئله** | «۷۰٪ طلا» نمی‌گوید کدام طلا؛ تا این نباشد اثرِ سبد قابلِ محاسبه نیست |
| **سیستم‌ها** | `lib/core/allocation.ts` (موجود) · `intel_reference_*` (پشتِ WP-05) |
| **وابستگی** | `WP-05` · تصمیم‌های D-1..D-6 |
| **خارج از دامنه** | ساختِ موتورِ تخصیصِ تازه — `runAllocation` هست |
| **ریسک** | متوسط — انتخابِ ابزار پیامدِ مالی دارد |
| **پذیرش** | برای هر Sleeve یک ابزار با شاهدِ داده؛ `hasCompleteReferenceAllocation()` سبز؛ سبد در `runAllocation` بدونِ `dropped` اجرا شود |
| **تست** | اجرای تاریخیِ سبد روی `symbol_history`، با فروضِ صریح |
| **Rollback** | نسخهٔ سبد append-only است؛ نسخهٔ تازه جایگزینِ قبلی نمی‌شود |
| **مالکِ تصمیم** | **آرش** |

## WP-07 · میزِ روزانه روی شش سؤال

| قلم | مقدار |
|---|---|
| **هدف** | میز به هر شش سؤال با دادهٔ واقعی یا `unconfigured`ِ صادق پاسخ دهد |
| **مسئله** | PR #115 پوسته را ساخته؛ سؤال‌های ۱، ۲، ۴ و ۵ ورودیِ واقعی ندارند |
| **سیستم‌ها** | PR #115 (پس از merge) |
| **وابستگی** | `WP-05`, `WP-06` |
| **خارج از دامنه** | ساختِ میزِ سوم |
| **ریسک** | پایین |
| **پذیرش** | هر شش سؤال حالتِ صادق دارند؛ هیچ سؤالی صرفاً به‌خاطرِ وجودِ جدول سبز نیست؛ هر منبع مهرِ زمانیِ خودش را دارد |
| **تست** | گاردهای شکست‌پذیرِ #115 (۲۷ مورد) + گاردِ تازه برای هر سؤال |
| **Rollback** | میز افزایشی است؛ هر ناحیه جدا |
| **مالکِ تصمیم** | مهندسی |

## WP-08 · تمرینِ خصوصیِ ۱۰روزه

> `DD-022` (**DECIDED**) رونماییِ عمومی را به «حداقل ۱۰ روزِ کاریِ واقعیِ گردشِ
> داخلی» مشروط کرده و تصریح می‌کند «اعلامِ PASS با دادهٔ ساختگی ممنوع است».
> پس این بسته پیش‌نیازِ رونمایی است — و ترتیبِ قرارگرفتنش پیش از Agent
> توصیهٔ همین طراحی است، نه حکمِ `DD-022`.

| قلم | مقدار |
|---|---|
| **هدف** | ده روزِ **واقعی** گردشِ دستی، ثبت‌شده و اندازه‌گیری‌شده |
| **مسئله** | F-15: ابزار ساخته شده (`intel_rehearsal_days`, `POST /api/admin/intelligence/rehearsal`, نما) ولی هرگز اجرا نشده — و جدولش روی Production نیست |
| **سیستم‌ها** | `/admin/intelligence` · `phase22` |
| **وابستگی** | `WP-05` (جدول) · `WP-07` (میزِ قابلِ استفاده) |
| **خارج از دامنه** | هر خودکارسازی؛ کلِ نکتهٔ تمرین دستی‌بودن است |
| **ریسک** | پایین — فقط دادهٔ داخلی |
| **پذیرش** | ده ردیفِ واقعی با `brief_produced`, `minutes_to_approval`, `absent_sources`, `stale_sources`, `human_corrections`, `rejected_conclusions`, `missed_events`. **ردیفِ ساختگی ممنوع** |
| **تست** | خودِ تمرین تست است؛ اگر ده روز طول بکشد، Agent زود است |
| **Rollback** | لازم نیست |
| **مالکِ تصمیم** | آرش (اجرا) |

## WP-09 · نگاشتِ خروجی به سطحِ دسترسی

| قلم | مقدار |
|---|---|
| **هدف** | هر خروجی دقیقاً یک سطحِ دسترسی داشته باشد |
| **مسئله** | F-14: مدلِ سه‌سطحی پیاده شده ولی فقط دو مصرف‌کننده دارد |
| **سیستم‌ها** | `lib/access.ts` · `entitlements` |
| **وابستگی** | `WP-07` |
| **خارج از دامنه** | **پرداخت و اعطای دسترسی (PR #113)** |
| **ریسک** | متوسط — نشتِ خروجیِ خصوصی به سطحِ عمومی |
| **پذیرش** | جدولِ «خروجی → سطح»؛ تستی که ثابت کند خروجیِ خصوصی در سطحِ `visitor` رندر نمی‌شود؛ هیچ ماژولی پیش‌فرضِ همه نیست |
| **تست** | تستِ رفتاری برای هر سه سطح، مثلِ `lib/desk/access.test.ts` |
| **Rollback** | برگرداندنِ کامیت |
| **مالکِ تصمیم** | آرش (مرزِ محصول) |

## WP-10 · نخستین Agentِ کمکی

| قلم | مقدار |
|---|---|
| **هدف** | یک کمکِ محدود و قابلِ ممیزی، **پس از** اثباتِ گردشِ دستی |
| **مسئله** | امروز هیچ پایه‌ای برای ارزیابیِ Agent نیست |
| **وابستگی** | `WP-08` — ده روزِ واقعی **باید** کامل شده باشد. به‌علاوه `D-022` (منابعِ خبریِ مجاز و اینکه خلاصهٔ خودکار زیرِ نامِ آرش مجاز است یا نه) و `D-023` (تأمین‌کنندهٔ LLM زیرِ محدودیتِ شبکه) — **هر دو `OPEN`** |
| **خارج از دامنه** | هر انتشارِ خودکار · هر تصمیمِ سبد |
| **ریسک** | بالا |
| **پذیرش** | خروجیِ Agent همیشه `pending_approval` است؛ هر اجرا در `intel_runs` با `model/prompt/config hash` ثبت می‌شود؛ فقط `qualitativeMask` به LLM می‌رود، هرگز عددِ خامِ مالی (`CLAUDE.md`) |
| **تست** | گاردِ شکست‌پذیر: خروجیِ Agent نتواند منتشر شود |
| **Rollback** | خاموش‌کردنِ Agent؛ گردشِ دستی دست‌نخورده می‌ماند |
| **مالکِ تصمیم** | **آرش** |

---

## تصمیم‌هایی که فقط آرش می‌تواند بگیرد

به ترتیبِ فوریت. هیچ‌کدام کارِ مهندسی نیست:

| # | تصمیم | چه چیزی را باز می‌کند |
|---|---|---|
| ۱ | `phase20/21/22/23` روی Production اجرا شوند؟ | `WP-06` تا `WP-10` — پنج بسته |
| ۲ | نگاشتِ ابزارِ هر Sleeve (D-1) | اثرِ سبد، سؤالِ ۵ میز |
| ۳ | دادهٔ IME نگه داشته شود یا نوشتنش متوقف؟ | `WP-02` |
| ۴ | بازهٔ تاکتیکی، محرکِ بازتوازن، Risk Budget، Kill Switch (D-2..D-5) | سیاستِ سبد |
| ۵ | سبدِ مرجع برای چه کسی منتشر می‌شود؟ (D-6) | `WP-09` |
| ۶ | فیدِ تورم و درآمد ثابت تأمین شود؟ | Benchmarkِ هدف |

## گیت‌ها

| گیت | شرط |
|---|---|
| G-1 | هر `UNVERIFIED` یا عدد گرفته یا صراحتاً پذیرفته شده |
| G-2 | هیچ تولیدکننده‌ای به مقصدِ ناموجود نمی‌نویسد |
| G-3 | هر جدول قراردادِ نگه‌داری دارد |
| G-4 | هر آستانه به عددِ واقعیِ کد وصل است |
| G-5 | تصمیمِ migration مکتوب است |
| G-6 | سبد بدونِ `dropped` اجرا می‌شود |
| G-7 | هر شش سؤالِ میز حالتِ صادق دارند |
| G-8 | ده روزِ واقعیِ تمرین ثبت شده |
| G-9 | خروجیِ خصوصی در سطحِ عمومی رندر نمی‌شود |
| G-10 | خروجیِ Agent نمی‌تواند خودکار منتشر شود |
