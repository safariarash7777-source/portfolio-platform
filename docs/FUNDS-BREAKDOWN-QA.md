# تفکیکِ صندوق‌ها — مشاهده، علتِ تأییدشده، اصلاح، شاهد

> بستهٔ «تفکیکِ دسته‌های صندوق در دیده‌بان». مبنا: `ddaeb97` (main).
>
> **کامیتِ سنجش:** `292a72b` — همهٔ نتایجِ این سند روی همین درخت اندازه‌گیری
> شده‌اند. این سند هیچ کدی را عوض نمی‌کند.
>
> **شاهدِ داده:** توزیعِ سیزده نوعِ واقعیِ فید از اسنپ‌شاتِ پروداکشن
> (`ir_market_snapshots`، کلید `latest`، تاریخِ منبع ۱۴۰۵/۰۶/۲۴، ۳۳۰ صندوق)
> که در بستهٔ قبلی با SQL استخراج و در `lib/core/fundCategory.test.ts` ثبت شده
> است. هیچ عددی برای «شبیه‌شدن به انتظار» تغییر نکرده.

---

## ۱. جدولِ یافته‌ها

| # | مشاهده | علتِ تأییدشده | اصلاح | شاهد پس از اصلاح |
|---|---|---|---|---|
| ۱ | «سایر» با ۴۴ صندوق دومین دستهٔ بزرگِ دیده‌بان بود | `FUND_CATEGORIES` شش عضو داشت؛ نقره (۱۴)، مختلط (۹)، املاک (۸)، فراصندوق (۳) و کالایی (۲) همگی به «سایر» می‌ریختند و راهِ رسیدن نداشتند | یازده دستهٔ واقعی؛ «سایر» فقط جسورانه (۴) + خصوصی (۳) + پروژه (۱) | `fundCategory.test.ts` — `EXPECTED_COUNTS` با `deepEqual` روی کلِ نقشه؛ جمع = ۳۳۰ |
| ۲ | یک قاعدهٔ عامِ `/کالایی/` می‌توانست طلا و نقره را هم ببلعد | نامِ رسمیِ هر سه با «صندوق کالایی…» شروع می‌شود (`مبتنی بر طلا`، `مبتنی بر نقره`، `کشاورزی`) | ترتیبِ قواعد تثبیت شد: نقره → طلا → … → کالایی، با تستِ اختصاصی | تستِ «طلا و نقره زیرِ «کالایی» بلعیده نمی‌شوند» |
| ۳ | چیپِ «طلا ۳۵» با جست‌وجوی فعال ۳ ردیف می‌داد | جامعهٔ شمارشِ چیپ `funds` بود ولی جامعهٔ جدول `filtered` | هر دو روی `searchScoped` (پس از جست‌وجو، پیش از فیلترِ دسته) | کلیک روی «نقره» → `?type=نقره` و **۱۴ ردیف**، هم‌خوان با عددِ چیپ |
| ۴ | جمعِ درصدهای نوشته‌شده ۱۰۱ می‌شد | هر سهم جداگانه گِرد می‌شد (۲۸+۲۷+۱۹+۱۱+۴+۳+۳+۲+۱+۱+۲) | `sharePercent` با روشِ بزرگ‌ترین باقی‌مانده در هسته | تستِ «درصدهای نوشته‌شده دقیقاً ۱۰۰ می‌شوند»؛ در اسکرین‌شات: ۲۸+۲۷+۱۹+۱۰+۴+۳+۳+۲+۱+۱+۲ = ۱۰۰ |
| ۵ | کارتِ سنجه «۴۱۵٫۴ **همت تومان**» می‌نوشت | `formatTomanShort` در پلهٔ «همت» هم پسوندِ « تومان» می‌گذاشت؛ «همت» خودش مخفّفِ «هزار میلیارد تومان» است | پسوند فقط برای پله‌های زیرِ همت | `format.test.ts` — `assert.ok(!out.includes("همت تومان"))` |

---

## ۲. آنچه عمداً انجام **نشد**

- **چیپِ «شاخصی»** که در تابلوهای مرجع هست: هیچ‌یک از سیزده نوعِ فیدِ ما چنین
  نوعی ندارد (صندوقِ شاخصی زیرِ «صندوق سرمایه گذاری در سهام» می‌آید). دستهٔ
  همیشه‌خالی یعنی فیلتری که هرگز نتیجه ندارد.
- **کارتِ «جمعِ دارایی‌ها» (AUM)**: `assetB` در **۰ از ۳۳۰** صندوق موجود است.
  نزدیک‌ترین عددِ صادقی که داریم «ارزشِ بازارِ نمادها» است و همان نوشته شده،
  با یادداشتِ صریح که این دو یکی نیستند.

---

## ۳. آنچه بررسی **نشد**

- **اجرا روی دادهٔ زندهٔ BrsApi/Supabase**: شبکهٔ این محیط `supabase.co` و
  دامنه‌های خروجی را می‌بندد (۴۰۳ از پراکسی). نما با یک **fixtureِ محلی** روی
  `127.0.0.1:4599` رانده شد که توزیعِ سیزده‌نوعیِ بالا را بازمی‌سازد؛ این
  fixture در مخزن نیست و مسیرِ آزمایشی وارد نسخهٔ نهایی نشده.
- عددهای ریالیِ fixture ساختگی‌اند، پس «۴۱۵٫۴ همت» در اسکرین‌شات‌ها فقط
  صحتِ **قالب و واحد** را نشان می‌دهد، نه بزرگیِ واقعیِ بازار.
- **پیش‌نمایشِ Vercel** با دادهٔ واقعی اجرا نشد و پاسخ و زمانش مشاهده نشده.

---

## ۴. سنجه‌های سنجیده‌شده (۱۴۴۰×۹۰۰ و ۳۹۰×۸۴۴)

| سنجه | نتیجه |
|---|---|
| تعدادِ چیپ | ۱۲ (همه + یازده دسته) — دستهٔ بی‌صندوق رندر نمی‌شود |
| ارتفاعِ چیپ | ۴۸ پیکسل (کفِ ۴۴) |
| ارتفاعِ ردیفِ تفکیک | ۷۳ پیکسل |
| اسکرولِ افقی در ۳۹۰ پیکسل | ۰ |
| تمِ تیره | پنل با همان توکن‌ها رندر می‌شود (`breakdown-dark`) |
| `test:core` / `test:calc` | ۱۰۶۰/۱۰۶۰ · ۷۹/۷۹ |
| `lint` / `build` | سبز |

---

## ۵. خارج از دامنه — گزارش، بدونِ دست‌زدن

- در `HEAD` یک **سیم‌لینکِ خودارجاع** به نامِ `node_modules` ردیابی شده است
  (`120000`، مقصد: `/home/user/portfolio-platform/node_modules`). `npm install`
  آن را برمی‌دارد و درخت را کثیف نشان می‌دهد. این کامیت به آن دست نزده است.
- `assetB` هرگز از رله نمی‌آید (۰ از ۳۳۰) و NAV فقط برای ۳۷ از ۳۳۰ صندوق
  موجود است — هر دو از بستهٔ قبلی گزارش‌شده و هنوز پابرجا.
