# کارتِ اجرای بکاپ و بازیابی — یک صفحه، آمادهٔ اجرا

> **این سند دستور نمی‌دهد که «الان اجرا کن».** آماده است تا هر وقت آرش خواست،
> بدونِ فکرکردن به جزئیات، اجرا شود. **هیچ بکاپی در این جلسه گرفته نشد.**
>
> رویهٔ کاملِ ویندوز: [`../RUNBOOK-backup-windows.md`](../RUNBOOK-backup-windows.md)

---

## ۰. چرا این دروازه هست

سازمانِ Production روی پلنِ **رایگان** است و Supabase برایش بکاپِ خودکار
نمی‌گیرد. تا نقطهٔ بازگشتِ **اثبات‌شده** نداشته باشیم، هیچ migrationی روی
Production اجرا نمی‌شود. این قاعده دور زده نمی‌شود.

**الزام «ویندوز» از هیچ دستوری نیامده.** بررسیِ مستند: `scripts/backup-production.sh`
همان کار را روی هر میزبانِ یونیکسیِ دارای Docker انجام می‌دهد. الزامِ واقعی این
است: **یک اپراتورِ انسانی که رمزِ دیتابیس دستش است، روی ماشینی با Docker.**
ویندوز صرفاً ماشینِ آرش است.

**چرا من نمی‌توانم اجرا کنم:** این کانتینر TCPِ مستقیم به دیتابیس ندارد، Docker
daemon ندارد، و — تعیین‌کننده‌تر — **رمز باید دستِ اپراتور بماند و هرگز وارد چت
نشود.** پس این کار ذاتاً دستِ مالک است، نه یک محدودیتِ ابزاری.

---

## ۱. پیش‌نیاز (یک‌بار)

| ابزار | چرا |
|---|---|
| **Docker Desktop** (باز و بالا آمده) | هم بکاپ می‌گیرد، هم بازیابی را در محیطِ ایزوله می‌آزماید |
| **Docker Desktop → Settings → Docker Engine**: افزودنِ `"ip": "127.0.0.1"` → Apply & restart | Supabase CLI پورت‌های استکِ موقت را پیش‌فرض روی **همهٔ** رابط‌ها باز می‌کند (یافتهٔ Codex). اسکریپت پیش از واردکردنِ دادهٔ واقعی پورت‌ها را می‌خواند و اگر یکی روی `0.0.0.0` یا `::` باشد، **متوقف می‌شود** — بدونِ این تنظیم به مرحلهٔ بازیابی نمی‌رسد |
| **Node.js LTS** | ابزارِ رسمیِ Supabase و `compare.mjs` |
| **git** | برای گرفتنِ **دقیقاً همان نسخهٔ آزموده** |
| **Supabase CLI به‌روز** | Authِ Production در طرحِ `20260831180000` است (نخستین انتشارِ Auth که آن را دارد: `v2.197.0`). اسکریپت نسخهٔ طرحِ Authِ Production را در `auth-version.txt` ثبت می‌کند و اگر Authِ استکِ محلی قدیمی‌تر باشد، **پیش از بازگردانی متوقف می‌شود** — با پیامِ «update the Supabase CLI» |

---

## ۲. دستور

روی **ویندوز**، در **Windows PowerShell** (نه لازم است PowerShell 7 باشد)، **داخلِ پوشهٔ ریپو**:

```powershell
git fetch origin claude/backup-stdin-crlf
git checkout --detach origin/claude/backup-stdin-crlf
git rev-parse --short HEAD        # باید با SHAِ اعلام‌شده در PR #155 یکی باشد
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\backup-production.ps1
```

بکاپ در `%USERPROFILE%\supabase-backups\prod-<زمان>` ساخته می‌شود — **بیرون از ریپو**؛
اگر مقصد داخلِ یک مخزنِ گیت باشد، اسکریپت اجرا نمی‌شود.

روی **مک یا لینوکس**:

```bash
bash scripts/backup-production.sh
```

اسکریپت رشتهٔ اتصال را **می‌پرسد** و آن را:
- روی دیسک نمی‌نویسد
- در تاریخچهٔ شل نمی‌گذارد
- در خروجی چاپ نمی‌کند

> ⚠️ **رشتهٔ اتصال را در چت نگذار.** نه اینجا، نه در هیچ پیامی. اسکریپت خودش
> از تو می‌گیرد.

رشته را از **Supabase Dashboard → پروژه → دکمهٔ Connect → Session pooler**
بردار.

---

## ۳. معیارِ موفقیت — «فایل هست» کافی نیست

اسکریپت خودش هر چهار مورد را می‌سنجد و اگر یکی رد شود، **کدِ خروجیِ ناصفر**
می‌دهد:

| # | معیار | چطور سنجیده می‌شود |
|---|---|---|
| ۱ | بازگردانی در **یک تراکنش** انجام شد | `ON_ERROR_STOP=1` + **کدِ خروجیِ `psql`** — نه grep روی لاگ |
| ۲ | مقصد یک **استکِ Supabaseِ محلیِ ایزوله** بود | نه Postgresِ ساده — وگرنه اسکیماهای `auth`/`storage` اصلاً آزموده نمی‌شوند |
| ۳ | **شمارشِ دقیقِ** ردیف‌های هر جدول برابر است | `scripts/backup/inventory.sql` — `count(*)`، نه `reltuples` |
| ۴ | اثرِ انگشتِ ساختاری در **هر دو جهت** برابر است | `scripts/backup/compare.mjs` — هر موردِ غایب، اضافه یا با تعریفِ متفاوت ⇒ کدِ ۱ |

`compare.mjs` فقط با **صفر اختلاف در هر دو جهت** کدِ ۰ می‌دهد
(`scripts/backup/compare.mjs:129`).

---

## ۴. خروجیِ سبز چه شکلی است

```
✅ <N> مورد بررسی شد · صفر اختلاف در هر دو جهت.
```

و کدِ خروجیِ کلِ اسکریپت `0`.

هر چیزِ دیگری یعنی **بکاپ قابلِ اتکا نیست** و دروازه باز نشده.

---

## ۵. بعد از سبزشدن

۱. تاریخ و مسیرِ فایل را به آرش گزارش کن (**نه محتوا، نه رشتهٔ اتصال**).
۲. آن‌وقت — و فقط آن‌وقت — ترتیبِ migrationهای `docs/MARKET-CORE-AUDIT.md` §۴.۴
   قابلِ اجراست، با تصمیمِ جداگانهٔ آرش برای هر کدام.

⚠️ فایل‌های بکاپ **بیرون از مخزن** ساخته می‌شوند و اسکریپت اگر ببیند مقصد داخلِ
یک ریپوی گیت است اجرا نمی‌شود. هرگز بکاپ را commit نکن.

---

## محدودیتِ آزمونِ این نسخه — صریح

| چه آزموده شد | کجا | چه **آزموده نشد** |
|---|---|---|
| کلِ مسیرِ ps1 تا PASS (اتصال → dump → استکِ موقت → بررسیِ پورت → بازگردانی با `supabase_admin` داخلِ کانتینر → مقایسه) | لینوکس، **PowerShell 7.4**، Postgresِ واقعی؛ `docker` و `supabase` جعلی | **Windows PowerShell 5.1 واقعی** اجرا نشد |
| رفتارِ خطِ فرمانِ 5.1 (نقل‌قولِ دوتایی escape نمی‌شود) | همان آزمون با `$PSNativeCommandArgumentPassing='Legacy'`؛ نسخهٔ قبل در همین حالت با رشتهٔ اتصالِ **درست** «Could not connect» می‌داد | کدگذاریِ کنسولِ ویندوز و سیاستِ اجرای اسکریپت |
| BOM: `pgurl.sh` و `compare.mjs` BOM را برمی‌دارند؛ هیچ فایلِ خروجی BOM ندارد | آزمونِ ورودیِ BOM+CRLF روی busybox/dash و Postgresِ واقعی؛ گاردِ ایستا روی `Set-Content -Encoding UTF8`، `| Set-Content` و `*>` | نوشتنِ BOM توسطِ خودِ 5.1 (روی 7.4 بازتولیدپذیر نیست) |
| توقف پیش از بازگردانی اگر Authِ مقصد قدیمی‌تر از Production باشد؛ `auth.schema_migrations` و `storage.migrations` از شمارش بیرون‌اند (دادهٔ خودِ سرویسِ مقصد، نه بکاپ) | e2e با Authِ قدیمی‌تر؛ آزمونِ یکپارچه با Authِ تازه‌تر (PASS) و یک ردیفِ کم در `auth.users` (FAIL) | نسخهٔ Authِ همراهِ Supabase CLIِ نصب‌شده روی لپ‌تاپ |
| توقف پیش از بازگردانی اگر پورتی روی `0.0.0.0`/`::` باشد | آزمونِ IPv4 و IPv6 با خروجیِ جعلیِ `docker ps` | اینکه تنظیمِ `"ip": "127.0.0.1"` در Docker Desktop واقعاً پورت‌های Supabase CLI را محدود کند — اسکریپت آن را **می‌سنجد** و در غیرِ این صورت متوقف می‌شود |
| — | — | **هیچ بکاپی از Production** گرفته نشده |
