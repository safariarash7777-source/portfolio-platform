# راهنمای شروعِ کار — پلتفرم آرش صفری («قطب‌نمای بازار»)

> این سند برای دولوپرِ جدید (انسان یا ایجنت) است تا **بدونِ پرسیدن** کلِ تصویر را بگیرد.
> اگر فقط ۵ دقیقه وقت داری: بخش **۱ (محصول)** و **۲ (قوانینِ سخت)** را بخوان — نقضِ بخش ۲
> یعنی خرابیِ حقوقی/اعتمادی، نه فقط باگ.
>
> **اگر می‌خواهی بدانی «الان کجاییم و چه چیزی گیر است» → اول
> [`COMMAND-CENTER.md`](./COMMAND-CENTER.md) را بخوان، نه این فایل را.**
> این سند «پروژه چیست و چطور کار می‌کند» را می‌گوید؛ آن سند «امروز چه خبر است» را.

## نقشهٔ اسناد — کدام سؤال، کدام فایل

| سؤالت این است | برو سراغ |
|---|---|
| الان کجاییم؟ چه چیزی بلاک است؟ کدام PR باز است؟ | [`COMMAND-CENTER.md`](./COMMAND-CENTER.md) — **وضعیتِ عملیاتیِ زنده** |
| چه تصمیمی گرفته شده و چه تصمیمی هنوز باز است؟ | [`DECISION-LOG.md`](./DECISION-LOG.md) — دفترِ دائمیِ تصمیم‌ها |
| چرا این تصمیمِ معماری گرفته شد؟ گزینه‌ها چه بود؟ | [`ADR/`](./ADR/) — ADR-001 میزبانیِ مینی‌اپ · ADR-002 مالکیتِ scheduler · ADR-003 منبعِ حقیقتِ لید |
| کدام migration واقعاً اجرا شده؟ | [`MIGRATION-LEDGER.md`](./MIGRATION-LEDGER.md) — **منبعِ حقیقتِ migration** |
| مالکِ این سرویس کیست؟ | [`SERVICE-OWNERSHIP.md`](./SERVICE-OWNERSHIP.md) |
| این متغیرِ محیطی کجا استفاده می‌شود؟ | [`ENVIRONMENT-MATRIX.md`](./ENVIRONMENT-MATRIX.md) — **فقط نام، بدونِ مقدار** |
| معماریِ کلان چیست؟ چه چیزی کجا میزبانی می‌شود؟ | [`PRODUCTION-ARCHITECTURE.md`](./PRODUCTION-ARCHITECTURE.md) |
| پروژه چیست و چطور رویش کار کنم؟ | همین فایل |
| وضعیتِ DSS/ترمینال/کوانت؟ | [`DSS-STATE.md`](./DSS-STATE.md) |

---

## ۱) محصول چیست

پلتفرمِ **تحلیل ریسک و طراحی پرتفوی** برای بازار سرمایهٔ ایران، ساختهٔ **آرش صفری**
(مشاور سرمایه‌گذاریِ دارای مجوز). هستهٔ فکری: **سامانهٔ پشتیبانِ تصمیم (DSS)**، نه رباتِ
معامله‌گر. یعنی داده و تحلیل می‌دهد تا کاربر خودش تصمیم بگیرد؛ **لایهٔ اجرا/سفارش ندارد.**

تمایزِ اصلی (چیزی که رقبا مثل انیگما/بورس‌ویو ندارند): **صداقتِ دادهٔ اثبات‌پذیر** —
عددِ ناموجود = خالی، بازهٔ ارزش‌گذاری به‌جای قیمت هدف، و «کارنامهٔ هش‌زنجیره‌ای» از
تحلیل‌های آرش (هنوز ساخته نشده؛ بخش ۱۱).

مخاطب: سرمایه‌گذارِ ایرانی. کلِ UI **فارسی و راست‌به‌چپ** است.

---

## ۲) قوانینِ سخت (نقض‌ناپذیر)

اینها از `~/.claude/CLAUDE.md` (شخصیِ آرش) و `CLAUDE.md`ِ ریپو می‌آیند و **بر هر چیزِ دیگر
اولویت دارند**:

1. **هیچ عدد/آمار/درصد/تستیمونیالِ ساختگی.** دادهٔ واقعی و اثبات‌پذیر یا **`null`**. این
   یک مسئولیتِ **حقوقی** است، نه سلیقه. (کلِ تصمیمِ اخیرِ ما دربارهٔ درج‌نکردنِ یک عددِ
   PSY مشکوک از همین‌جا آمد — بخش ۸.)
2. **هیچ سود تضمینی/وعدهٔ بازدهی**، نه صریح نه تلویحی.
3. **واژه‌های «سیگنال/خرید/فروش/توصیه/پیشنهاد» در هیچ خروجیِ UI یا متنِ تولیدی مجاز نیست.**
   (این DSS است، نه توصیه‌گر.) «تحلیل» بگو.
4. **ارزش‌گذاری همیشه بازه + فروض (سه سناریو)** — هرگز قیمت هدفِ واحد.
5. **جدول‌های داده append-only / نسخه‌دار.** `UPDATE`/`DELETE`ِ مخرب که رکوردِ قبلی را
   نابود کند ممنوع (به‌ویژه `codal_reports`, `symbol_history`, `signals`, `fx_heavy_analytics`).
6. **سکرت‌ها هرگز چاپ/کامیت نشوند.** فقط `.env` و placeholder. `admin.ts` (سرویس‌رول)
   **هرگز** در کامپوننتِ کلاینت import نشود (کلید لو می‌رود).
7. **بدون اجازهٔ صریحِ آرش در همان جلسه: push/PR/deploy نکن.** تغییر را اعمال و **local
   کامیت** کن؛ منتظرِ okِ او بمان.
8. **ویرایشِ جراحی.** فقط همان چیزی که تسک خواسته؛ به تم/چیدمان/منطقِ نامرتبط دست نزن.
   قبل از بازنویسیِ گسترده/rename/افزودن dependency بپرس.
9. **پاسخ‌ها فارسی.** اصطلاحِ فنیِ انگلیسی را بارِ اول یک‌خطی معنی کن (آرش مشاور است، نه
   توسعه‌دهنده).
10. **قبل از کار روی DSS/ترمینال/کوانت** → `docs/DSS-STATE.md` را بخوان.
    **قبل از کار روی داده/رله/نماد/صندوق/NAV/کدال** → `.claude/skills/iran-market-data/SKILL.md`.

---

## ۳) استک فنی

- **Next.js 15** (App Router) · **React 18** · **TypeScript** · **Tailwind 3**
- **Supabase** (`@supabase/ssr`) — دیتابیس + Auth. بدونِ ORM؛ دسترسیِ مستقیم با کلاینتِ Supabase.
- نمودار: **`recharts`** + **`lightweight-charts`** · آیکون: `lucide-react`
- فونت **Vazirmatn** با `next/font`، **سلف‌هاست** (هرگز از CDN لود نکن).
- **رله (relay/)**: یک اپِ Node مستقل روی **Liara** (PaaS ایرانی) که از داخلِ ایران دادهٔ
  بازار را از BrsApi می‌گیرد و به Supabase می‌نویسد (چون APIهای داخلی IP خارجی را بلاک می‌کنند).
- **پایتون** (فقط برای مدل‌های سنگینِ اقتصادسنجیِ ارز — بخش ۸): numpy/scipy/statsmodels/arch.
- دیپلوی: **Vercel** (سایت) + **Liara** (رله).

---

## ۴) معماری — سه اصلِ کلیدی

**الف) «یک موتور، دو نما».** همهٔ محاسباتِ هسته فقط در `lib/core/engine.ts` (و دوستانش در
`lib/core/`) است. ترمینالِ ادمین و صفحاتِ عمومی **هر دو از همان می‌خوانند** — منطق دوباره
نوشته نمی‌شود. هر تابعِ هسته **قبل از UI تست دارد**: `npm run test:core` و `npm run test:calc`
(node:test + tsx). **VERIFIED (2026-07-25، روی `main`):** `test:core` = ۲۳۸ تست سبز،
`test:calc` = ۶۵ تست سبز (مجموع ۳۰۳)، هر دو با ۰ شکست.

**ب) سه‌گانهٔ Supabase (اشتباه نگیر):**
- `lib/supabase/client.ts` → کامپوننتِ کلاینت (مرورگر، کلیدِ anon).
- `lib/supabase/server.ts` → RSC و route handler (با کوکیِ کاربر).
- `lib/supabase/admin.ts` → **service-role**، `server-only`، RLS را دور می‌زند. فقط برای
  verifyِ پرداخت و وبهوکِ تلگرام و نوشتنِ سرور. **هرگز در کلاینت import نشود.**

**ج) جریانِ داده:**
```
BrsApi (داخل ایران) ──> رلهٔ Liara (relay/server.mjs) ──> Supabase (جدول‌های append-only)
                                                              │
                              سایتِ Vercel (RSC) ─── می‌خواند ─┘ ─── نمایش به کاربر
```
Authِ مسیرها در `middleware.ts`: `/dashboard`، `/admin`، `/terminal` گیت می‌شوند؛ نقشِ
admin از `profiles.role` در دیتابیس خوانده می‌شود (منبعِ واحدِ حقیقت).

---

## ۵) نقشهٔ ریپو

```
app/                 صفحات (App Router)
  page.tsx           لندینگ
  (auth)/            login/register/reset
  (protected)/       dashboard، admin/*، terminal/*  (پشتِ گیت)
    admin/fx/        ← داشبورد ارز (بخش ۸)
    admin/radar/     ← پنل رصد بازار (اسکرینر گلوگاه)
  market/            بازار عمومی: map/stocks/funds/options
  data/ symbol/      صفحهٔ نماد (بانک داده)
  analyses/ codal/ insights/ notes/ webinars/ learn/ legal/
components/          کامپوننت‌های UI (از جمله admin/AdminFxDashboard.tsx)
lib/
  core/              «موتور» — همهٔ محاسبات + تست‌ها (engine, regime, backtest, screener, ...)
  fx/                موتور و دادهٔ داشبورد ارز (بخش ۸)
  fundamental/       کارت بنیادی نماد + پارسر کدال
  supabase/          سه‌گانهٔ کلاینت/سرور/ادمین
  track/             زیرساختِ تحلیل‌ها + کارنامهٔ هش‌زنجیره‌ای (WP6 — هنوز بی‌UI)
  format.ts          هلپرهای فارسی: toPersianDigits, formatToman, formatJalali (حتماً استفاده کن)
  market-ir.ts, market.ts, telegram-*, zarinpal.ts, resend.ts, access.ts
relay/               اپ Node روی Liara (فید داده به Supabase)
scripts/             ابزارِ آفلاین — از جمله پایتونِ سنگینِ ارز (بخش ۸)
sql/                 مهاجرت‌ها/اسکیماها
docs/                مستندات (این فایل، DSS-STATE, PRODUCT-BLUEPRINT, BRIEF-MANUS, ...)
.claude/skills/      اسکیل‌های پروژه: iran-market-data (قواعدِ سختِ داده)، ui-ux-pro-max
middleware.ts        گیتِ auth
```
**رنگ/فونت فقط از توکن:** `tailwind.config.js` + متغیرهای `app/globals.css`
(`--navy`, `--gold`, `--surface`, `--line`, ...). هیچ رنگِ خارج از توکن ننویس.

---

## ۶) گیت — برنچ‌ها و جریان

**جریان:** `feature/* → develop → main`. `main` = production (Vercel).

| برنچ | نقش |
|---|---|
| `main` | production. دیپلویِ زندهٔ Vercel. |
| `develop` | ادغامِ فیچرها قبل از main. |
| `feat/fx-heavy-monthly` | **فعال، PR #74 باز → develop.** آخرین کارِ ارز (بخش ۸). |
| `feat/fx-heavy-analytics` | مرج‌شده (PR #73 → develop). فاز ۲ اولیهٔ ارز. |
| `claude/write-claude-md-files-0juicp` | برنچِ قدیمیِ توسعه؛ اسکیلِ manus-agent-methodology اینجا local کامیت شده (push نشده). |
| `claude/*` بقیه | برنچ‌های قدیمی/آزمایشی (relay-stocks، funds-board، ...). اکثراً بی‌استفاده. |

**قواعدِ کامیتِ ما:** هویت `Claude <noreply@anthropic.com>`؛ کامیت‌ها بدونِ امضای GPG
«Unverified» نشان داده می‌شوند (محدودیتِ محیط، عادی است). **push فقط با اجازهٔ آرش.**

**PRهای باز (اسنپ‌شات ۲۰۲۶-۰۷-۲۵ — این جدول سریعاً کهنه می‌شود؛ همیشه با GitHub چک کن):**
#74 (feat/fx-heavy-monthly → develop، پیش‌نویس)، #76 (chore/remove-duplicate-cron-workflows → main،
پیش‌نویس)، #77 (docs/production-architecture-baseline → main، پیش‌نویس). #73 مرج شد.

---

## ۷) دیتابیس (Supabase)

Project id: `uooeygybrniptzdxuzhj`. **همهٔ جدول‌ها RLS دارند.** مهم‌ها:

| جدول | کار |
|---|---|
| `ir_market_snapshots` | اسنپ‌شاتِ زندهٔ بازار (key=`latest`)؛ رله upsert می‌کند، سایت می‌خواند. seedهای داشبورد ارز هم اینجا زیرِ کلیدهای `fx_seed_*` ذخیره می‌شوند. |
| `ir_market_history` | تاریخچهٔ ۳۰دقیقه‌ای بازار (append-only). |
| `symbol_history` | تاریخچهٔ روزانهٔ هر نماد (OHLCV + پول حقیقی/حقوقی). ۲M+ ردیف. append-only. |
| `codal_reports` / `codal_feed` | گزارش‌ها و فیدِ کدال. append-only، dedup با `source_url`. |
| `fx_rates` | نرخ روزانهٔ دلار بازار آزاد (تومان). append-only. منبعِ نرخِ زندهٔ داشبورد ارز. |
| `index_history`, `market_breadth` | EOD شاخص و نبض بازار. append-only. |
| `signals` / `signal_outcomes` / `weekly_outlooks` | **کارنامهٔ هش‌زنجیره‌ای** (append-only + هش). جدول‌ها ساخته شده‌اند ولی **UI ندارند** (WP6، بخش ۱۱). |
| `fx_heavy_analytics` | **جدید.** نتایجِ پیش‌محاسبهٔ مدل‌های سنگینِ ارز. append-only + نسخه‌دار. RLS: خواندنِ ادمین، نوشتنِ service_role. (بخش ۸) |
| `profiles`, `entitlements`, `payments`, `webinars`, `telegram_*`, ... | کاربر/دسترسی/پرداخت/وبینار. |

قاعده: هر نوشتنِ کاربر باید **نسخه‌دار** باشد؛ upsertِ مخرب ممنوع.

---

## ۸) داشبورد ارز `/admin/fx` — کارِ فعالِ فعلی (مهم)

**چیست:** یک «ابر داشبوردِ اقتصادسنجیِ نرخ ارز»، **ادمین‌فقط**. نسخهٔ TypeScriptِ یک داشبوردِ
Streamlit پایتونیِ آرش. هدف: بدونِ سرورِ جدید و بدونِ هزینه، تحلیلِ ارز داخلِ همین سایت.

**دو نیمه:**

- **نیمهٔ سبک (زنده، در مرورگر) — کامل و تست‌شده.** `lib/fx/engine.ts`: PPP چندپایه،
  مدل پولی (+سرعت گردش)، فرنکل–بیلسون (سری)، ترکیبِ هندسی، حباب، پیش‌بینیِ رو به جلو،
  حساسیت (نفت/کسری/heatmap/تورنادو). **۲۶ تستِ عددیِ طلایی** مقابلِ خروجیِ پایتونِ آرش
  (`lib/fx/engine.test.ts`). دادهٔ کلان از `lib/fx/data/*.json` (سالانه/ماهانه)، نرخِ زندهٔ
  دلار از `fx_rates`. صفحه: `app/(protected)/admin/fx/page.tsx` → `components/admin/AdminFxDashboard.tsx`.
  آپلودِ اکسل از خودِ سایت (`FxSeedManager` → `ir_market_snapshots`).

- **نیمهٔ سنگین (پیش‌محاسبه) — درحالِ ساخت.** مدل‌هایی که در TS کتابخانه ندارند
  (PSY/GSADF، GARCH، مونت‌کارلو، هم‌جمعی). **معماری:** پایتون روی PC حساب می‌کند →
  ردیف در `fx_heavy_analytics` می‌گذارد → تبِ «آزمون‌های آماری» نمایش می‌دهد.
  - > ⚠️ **این اسکریپت‌ها روی `main` نیستند** (VERIFIED 2026-07-25). روی `main` پوشهٔ `scripts/`
    > فقط `fx-refresh-seeds.py` و `set-telegram-webhook.ts` را دارد. پس اگر روی `main` هستی،
    > دستورها و مسیرهای زیر اجرا نمی‌شوند — اول به برنچِ مربوطه سوییچ کن.
  - اسکریپت: `scripts/fx-precompute-heavy.py` (+ `scripts/requirements-fx-heavy.txt`,
    `scripts/README-fx-heavy.md`) — **روی `develop` و `feat/fx-heavy-monthly`**.
  - **`scripts/fx_psy.py` = پورتِ بایت‌به‌بایتِ `psy.py`ِ آرش** (**فقط روی `feat/fx-heavy-monthly`،
    PR #74 — نه روی `develop`، نه روی `main`**) (PSY 2015 صحیح: BSADF،
    انتخابِ وقفه با BIC، مقدارِ بحرانیِ دنباله‌ایِ شبیه‌سازی‌شده، قاعدهٔ حداقل‌مدت).
  - خودآزمونِ PPP قبل از هر اجرا مقابلِ `engine.ts` → **عدمِ واگراییِ محاسبات با سایت**.
  - GARCH و MC و هم‌جمعی: سالم و درج‌شده (نوسانِ جاریِ سالانه ~۲۱٪؛ IGARCH یادداشت شده).

**وضعیتِ صادقانهٔ فعلی (خیلی مهم برای دولوپرِ جدید):**
- ✅ کد (سبک + پورتِ psy.py + نمودار BSADF + جدول + GARCH/MC) آماده و سبز (`build`/`typecheck`).
- ⚠️ **عددِ PSY هنوز درج‌کردنی نیست.** چرا: PPP ماهانه از **درون‌یابیِ سالانه** ساخته می‌شود،
  چون **CPI ماهانهٔ ایرانِ قبل از ۱۳۹۹** را نداریم. این باعث شد آزمون، دورهٔ ۱۳۸۵–۸۹ را
  «حباب» علامت بزند درحالی‌که نسبت آنجا **نزولی** بود → آرتیفکت. طبق قانونِ ۱، درج نشد.
- **بلاکر:** فایلِ **«اکسل سری کامل تورمِ ایران»** (ماهانه، پیش از ۱۳۹۹) لازم است تا PPP
  ماهانهٔ **واقعی** ساخته شود. یا PSY فقط روی پنجرهٔ ۱۳۹۹+ (که CPI واقعی داریم) اجرا شود.
- **بلاکر ۲ (برای §۱.۵ و BEER/ECM):** فایلِ **`econometrics.py`**ِ آرش (برآوردِ واقعیِ
  ضرایبِ OLS و BEER/ECM). `models.py` فقط wrapper بود.

**قاعدهٔ نمایش:** تبِ آمار باید همیشه **«محاسبه‌شده در تاریخِ …»** را نشان دهد (این نتایج
زنده نیستند)، قابِ **توصیفی** (نه توصیه)، و بخشِ غایب = `null` صادق با ذکرِ دلیل.

---

## ۹) محیط، سکرت‌ها، اجرا

`.env` از روی `.env.example` (این فایل commit **نشود**). مهم‌ها:
`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` (عمومی)،
`SUPABASE_SERVICE_ROLE_KEY` (فقط سرور)، زرین‌پال/تلگرام/Resend/CRON (فقط سرور).
برای اسکریپتِ پایتونِ ارز: `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` در `.env`.

```bash
npm run dev        # توسعهٔ محلی
npm run build      # باید سبز باشد — پایانِ هر کار
npm run typecheck  # اگر تایپ‌ها را لمس کردی
npm run lint       # ESLint CLI — غیرتعاملی
npm run test:core  # موتورِ هسته
npm run test:calc  # محاسبات بنیادی/تقویم/…

npm run lint:ci            # همان lint ولی warning هم مردود است
npm run scan:secrets       # اسکنِ الگوی سکرت
npm run validate:sql       # بررسیِ سیاستیِ فایل‌های sql/ (بدونِ اجرا)
npm run validate:sql:grammar  # گرامرِ واقعیِ Postgres (نیازمندِ `pip install pglast`)
```

> ✅ **رفع شد (2026-07-25، P1-010):** `npm run lint` دیگر تعاملی نیست. `next lint`
> منسوخ با **ESLint CLI + flat config** (`eslint.config.mjs`) جایگزین شد و `eslint`
> و `eslint-config-next` به devDependencies اضافه شدند. کدِ فعلی **۰ خطا** می‌دهد.
> همهٔ این دستورها حالا در CI روی هر PR اجرا می‌شوند (`.github/workflows/ci.yml`).

پایتونِ سنگینِ ارز (روی PC) — **فقط پس از سوییچ به برنچی که این فایل‌ها را دارد**
(`develop` یا `feat/fx-heavy-monthly`)؛ روی `main` وجود ندارند:

```bash
pip install -r scripts/requirements-fx-heavy.txt
python scripts/fx-precompute-heavy.py --usd-csv dollar_monthly.csv   # + .env با service_role
```

---

## ۱۰) وضعیت: زنده / درحال‌ساخت / بلاک

| بخش | وضعیت |
|---|---|
| بازار عمومی، بانک داده، صفحهٔ نماد، کدال v3، صندوق‌ها/NAV | زنده |
| موتورِ هسته + ۵۱ تست، رله، append-only | زنده و پایدار |
| داشبورد ارز — نیمهٔ سبک | زنده (روی `main`) |
| داشبورد ارز — نیمهٔ سنگین: GARCH/MC/هم‌جمعی | کد+درج آماده؛ روی `develop` (PR #73/#74) |
| داشبورد ارز — PSY (نمودار BSADF) | **کد آماده، عدد بلاک** (نبودِ CPI ماهانهٔ پیش از ۱۳۹۹) |
| §۱.۵ فرنکل–بیلسون OLS + BEER/ECM | بلاک (نبودِ `econometrics.py`) |
| WP6: تحلیل‌های آرش + کارنامهٔ هش‌زنجیره‌ای | جدول‌ها ساخته، **UI صفر** — تمایزِ اصلیِ محصول |
| باگِ middleware: مشتریِ دارای entitlement به `/terminal` راه نمی‌یابد | باز (مسیرِ درآمد) |

---

## ۱۱) تصمیم‌ها/بلاکرهای بازِ آرش

1. **اکسل تورمِ ماهانهٔ ایران** (پیش از ۱۳۹۹) + **`econometrics.py`** → تکمیلِ PSY و §۱.۵/BEER/ECM.
2. **merge به `main`** برای داشبورد ارز: **معلق** تا PSY معتبر شود (سیگنالِ مشکوک روی سایتِ زنده نرود).
3. **WP6 (کارنامه)**: تأییدِ انتشارِ عمومیِ کارنامه از روز اول + مستندسازیِ anchorهای امتیاز.
4. رفعِ **باگِ گیتِ ترمینال** (اولویتِ درآمد).
5. توکنِ گیت‌هابِ لو‌رفته در پوشه‌های قدیمیِ PC (باید revoke شود — کارِ آرش).

---

## ۱۲) هماهنگی — دو ایجنت هم‌زمان

گاهی **دو Claude** روی این پروژه کار می‌کنند: یکی اینجا (متصل به ریپو/Supabase/سایت)، یکی
روی **PCِ آرش** (متصل به فایل‌های محلی + tgju + کلیدِ service_role). **خطر: تصادم روی یک
فایل/جدول.** قاعده‌ای که با آرش گذاشتیم: **یک مالکِ واحد در هر لحظه.** اگر تقسیمِ کار عوض شد،
اول با آرش تأیید کن. جدولِ `fx_heavy_analytics` **append-only** است، پس ردیف‌های موازی
همدیگر را خراب نمی‌کنند ولی واگراییِ کد و ردیف‌های تکراری بد است — هماهنگ بمان.

---

## ۱۳) چک‌لیستِ شروع (دولوپرِ جدید)

0. **`docs/COMMAND-CENTER.md` را بخوان** — وضعیتِ عملیاتیِ امروز، بلاکرهای باز، PRهای باز.
   بدونِ آن نمی‌دانی چه چیزی همین حالا شکسته است.
1. این سند + بخشِ ۲ (قوانین) + `CLAUDE.md`ِ ریپو + `~/.claude/CLAUDE.md`ِ آرش را بخوان.
2. `docs/DSS-STATE.md` و `docs/PRODUCT-BLUEPRINT.md` را بخوان (چشم‌انداز و وضعیتِ زنده).
3. اسکیلِ `.claude/skills/iran-market-data/SKILL.md` را بخوان (قواعدِ سختِ داده).
4. `npm ci && npm run build && npm run test:core` — همه باید سبز شوند.
   (**`npm ci` از P1-010 کار می‌کند.** پیش‌تر با `Missing: xlsx@0.18.5 …` می‌شکست چون
   `xlsx` و ۸ وابستگیِ فرعی‌اش در `package-lock.json` نبودند؛ لاک‌فایل بازتولید شد.
   از این پس **`npm ci` بزن نه `npm i`** — همان چیزی که CI اجرا می‌کند.)
5. برای داشبورد ارز: `scripts/README-fx-heavy.md` را بخوان — **این فایل روی `main` نیست**؛
   اول به `develop` یا `feat/fx-heavy-monthly` سوییچ کن (بخش ۸).
6. قبل از هر تغییر: مطمئن شو کدام برنچ (`develop`-محور) و کدام مالک (بخش ۱۲).
7. **push/PR/deploy نکن مگر آرش صریحاً بگوید.**

> سؤال داشتی، از خودِ آرش بپرس — او مشاورِ سرمایه‌گذاری است نه توسعه‌دهنده، پس
> سؤالِ فنی را ساده و با یک پیشنهادِ پیش‌فرض بپرس.

---

## ۱۴) چرا ابزارها گاهی دربارهٔ همین فایل هشدار می‌دهند

اگر ابزاری (ایجنت، ادیتور، اسکریپت) هشدار داد که `docs/ONBOARDING.md` گم شده، تغییر کرده،
یا ردیابی نمی‌شود، **قبل از هر کاری اول برنچت را نگاه کن.**

**ریشه (VERIFIED 2026-07-25):** این فایل **فقط روی برنچِ
`docs/production-architecture-baseline`** وجود دارد (با کامیتِ `034eb29` اضافه شد). روی
`main` — و روی هر برنچی که از `main` گرفته شده — **اصلاً وجود ندارد**. پس هر ابزاری که
مسیرِ `docs/ONBOARDING.md` را در حافظه دارد و بعد worktree به `main` سوییچ می‌کند، فایل را
گم‌شده می‌بیند و هشدار می‌دهد. این **عدمِ تطابقِ برنچ** است، نه خرابیِ فایل.

**آنچه بررسی شد و ریشه نبود:**

| احتمال | نتیجه |
|---|---|
| git hook | `.git/hooks/` فقط `*.sample` دارد — git هیچ‌کدام را اجرا نمی‌کند. `core.hooksPath` هم ست نیست. |
| Husky / lint-staged / pre-commit / lefthook | هیچ‌کدام نصب نیست (نه `.husky`، نه در `devDependencies`، نه اسکریپتِ `prepare`). |
| هوکِ Claude Code | `.claude/settings.local.json` فقط `permissions` دارد؛ هیچ کلیدِ `hooks` نیست. |
| `.gitignore` / `.git/info/exclude` | `git check-ignore` می‌گوید ignore نشده؛ `exclude` خالی است. |
| `skip-worktree` / `assume-unchanged` | هیچ فایلی در index این پرچم‌ها را ندارد. |
| پایانِ خط (CRLF) یا مودِ فایل | `.gitattributes` نداریم، `core.autocrlf` ست نیست، فایل UTF-8 با LF است، مود `100644` و منطبق با index. |
| فایلِ هم‌نامِ دیگر با کیسِ متفاوت | `find -iname '*onboarding*'` فقط همین یک فایل را پیدا می‌کند. |
| مسیرِ هاردکد در اسکریپتی از مخزن | هیچ فایلی در مخزن اصلاً رشتهٔ `ONBOARDING` را ندارد. |
| برنچِ tracking اشتباه | درست است: `origin/docs/production-architecture-baseline`. |
| تولیدشدن توسط اتوماسیون | نه — تنها کامیت‌هایش همان دو کامیتِ مستندسازی است. |

**روی برنچِ درست، فایل کاملاً سالم است:** پرچمِ `H` در `git ls-files -v`، و هشِ
worktree = هشِ HEAD = هشِ remote.

**کارِ درست:**
1. قبل از کار روی این فایل، `git branch --show-current` بزن و مطمئن شو روی
   `docs/production-architecture-baseline` هستی.
2. **رفعِ دائمی = merge شدنِ PR #77.** بعد از آن `docs/ONBOARDING.md` روی `main` هم هست و
   سوییچِ برنچ دیگر نمی‌تواند ناپدیدش کند.

**کارهایی که هرگز نباید بکنی:** این فایل را به `.gitignore` اضافه کنی، از ردیابی خارجش کنی،
یا هوکی را خاموش کنی. هیچ‌کدام ریشه را درست نمی‌کند — فقط علامت را پنهان می‌کند.
