# PRODUCT-BLUEPRINT.md — قطب‌نمای استراتژیک «قطب‌نمای بازار»

> **سند بالادستِ محصول.** «چه می‌سازیم، برای که، چرا، و به چه ترتیب» اینجاست.
> رابطه با بقیهٔ اسناد: «کجاییم/چه ساخته‌شده» → `DSS-STATE.md` · «قواعد سختِ داده» → اسکیل `.claude/skills/iran-market-data/` · «قوانین ریپو» → `CLAUDE.md` · «نقشهٔ صفحات» → `PRODUCT-MAP.md`.
> **هر فیچر/ابزار جدید، قبل از ساخت، باید با این سند سنجیده شود.**
> آخرین به‌روزرسانی: ۱۴۰۵/۰۴/۲۷.

---

## ۰) مقصد در یک جمله

> «قطب‌نمای بازار» = **خانهٔ کاملِ سرمایه‌گذارِ ایرانی.** جایی که یک سرمایه‌گذار — به‌ویژه **غیرحرفه‌ای** — همهٔ نیازهایش را **یک‌جا و قابل‌فهم** حل می‌کند؛ و این اعتماد از مسیرِ یک **قیف** به **مشاورهٔ آرش + ترمینال حرفه‌ای + وبینار** می‌رسد.

دو نیمهٔ مقصد، جدانشدنی:
- **جامعیت (مقصد):** کاربر با ورود بداند «هر چه برای سرمایه‌گذاری‌ام لازم دارم اینجاست و می‌فهممش».
- **قیف (مدلِ کسب‌وکار):** بخشِ عمومیِ رایگان اعتماد و SEO می‌سازد؛ درآمد از مشاوره/ترمینال/وبینار.

**جامعیت مقصد است، فازبندی مسیر.** سند کلِ خانه را ترسیم می‌کند تا هیچ نیازی جا نیفتد؛ فازها ترتیبِ ساختن را می‌دهند تا دوباره پراکنده نشویم.

**همیشه DSS است، نه ربات معامله‌گر؛ لایهٔ اجرا ندارد.**

---

## ۱) موقعیت‌یابی — چرا ما، در برابرِ انیگما/بورس‌ویو/رباتِ کدال۷۲۴

| | رقبا (انیگما، بورس‌ویو، رباتِ تلگرامیِ کدال۷۲۴) | ما |
|---|---|---|
| قدرتِ داده/ابزار | **بیشتر و پخته‌تر** — این میدانِ رقابت نیست | کافی، متمرکز |
| مخاطب | عمدتاً **حرفه‌ای**؛ غیرحرفه‌ای زیرِ انبوهِ عدد غرق می‌شود | **غیرحرفه‌ای در مرکز** — دستش را می‌گیریم |
| خروجی | برخی **«سیگنال/سبد پیشنهادی» می‌فروشند** | **هرگز سیگنال/توصیه** — تحلیل + بازه + فروض |
| اعتماد | برند/تبلیغ | **کارنامهٔ هش‌زنجیره‌ایِ راستی‌آزمایی‌پذیرِ یک مشاورِ دارای مجوز** |

**تمایزِ کپی‌ناپذیر:** ترجمهٔ داده به تصمیمِ قابل‌فهم + صدای مشاورِ مجوزدار با کارنامهٔ صادق. رقبا داده‌شان بیشتر است؛ این را نداریم — و لازم هم نداریم.

---

## ۲) نیازهای واقعیِ سرمایه‌گذار — محصول باید **هر هفت** را حل کند

از تازه‌وارد تا پیشرفته. این فهرست، معیارِ «جامعیت» است:

| # | نیاز | سؤالِ کاربر | پاسخِ محصول (ناحیه) |
|---|---|---|---|
| ۱ | **فهمیدن** | «این اصطلاح یعنی چه؟ از کجا شروع کنم؟» | سواد مالی، آموزشِ درجا (ناحیهٔ ۵) |
| ۲ | **جهت‌یابی** | «الان بازار در چه حالی است؟» | نبض بازار (ناحیهٔ ۱) |
| ۳ | **ارزیابی** | «این نماد/صندوق خوب است یا بد و *چرا*؟» | شناسنامهٔ نماد + کارت امتیاز (ناحیهٔ ۲) |
| ۴ | **تصمیمِ سبد** | «پولم را چطور تقسیم کنم؟ چقدر ریسک؟» | آزمون ریسک + تخصیص + سبد من (ناحیهٔ ۴) |
| ۵ | **پیگیری** | «چه خبر شد؟ نمادهایم چطورند؟» | دیده‌بان + هشدار + فید کدال (عرضی) |
| ۶ | **اعتماد/راهنمایی** | «به چه کسی تکیه کنم؟» | صدای مشاور + کارنامه + وبینار (ناحیهٔ ۳) |
| ۷ | **یادگیری از نتیجه** | «تصمیمم درست بود؟» | کارنامهٔ شفاف باز/بسته (ناحیهٔ ۳) |

---

## ۳) ساختار محصول — ۵ ناحیه + اصلِ دسترس‌پذیری

هر صفحهٔ موجود یا آینده باید دقیقاً ذیلِ یکی از این ناحیه‌ها بنشیند.

| # | ناحیه | سؤالِ کاربر | ابزارِ ویژهٔ غیرحرفه‌ای | نقش در قیف |
|---|---|---|---|---|
| ۱ | **نبض بازار** | «بازار امروز چه خبر است؟» | رژیمِ روزِ قابل‌فهم + روایتِ template | جذب · SEO |
| ۲ | **شناسنامهٔ نماد** | «این نماد را برایم بشکاف» | کارتِ بنیادیِ نمادمحور (الگو: رباتِ تلگرامیِ کدال۷۲۴) + امتیاز با driver | جذب/درگیرسازی |
| ۳ | **صدای مشاور** | «آرش چه می‌گوید، کارنامه‌اش چیست؟» | زبانِ ساده + کارنامهٔ شفاف | **اعتماد — قلبِ قیف** |
| ۴ | **ترمینال حرفه‌ای** | «ابزارِ تصمیمِ من کجاست؟» | آزمون ریسک + تخصیصِ راهنمادار | تبدیل · درآمد |
| ۵ | **یادگیری/سواد مالی** | «اصلاً اینها یعنی چه؟» | آموزشِ گام‌به‌گام + واژه‌نامه + وبینار | جذب/نگه‌داری |

**جریانِ قیف:**
```
 [۱ نبض بازار] ┐
 [۲ شناسنامه]  ├─→ جذبِ رایگان ─→ [۳ کارنامه] ─→ اعتماد ─→ [۴ ترمینال + مشاوره] ─→ درآمد
 [۵ یادگیری]  ┘        (SEO)          (اثباتِ اثرگذاری)              (اشتراک/وبینار)
```

### اصلِ دسترس‌پذیری (قانونِ عرضی روی **هر** ابزار)
هر داده/ابزار باید سه لایه داشته باشد — از ساده به خام:
1. **جوابِ یک‌خطیِ قابل‌فهم** (برای تازه‌وارد): «امتیازِ بنیادیِ این نماد متوسط است، چون حاشیهٔ سودش رو به کاهش است.»
2. **زمینه/چرایی**: driver، روند، مقایسهٔ تاریخی — عدد همیشه در زمینه، نه خام.
3. **دادهٔ خام**: آخرین لایه برای حرفه‌ای، نه ویترین.

+ واژه‌نامهٔ درجا (tooltip روی هر اصطلاح)، زبانِ ساده، بدونِ ژارگونِ بی‌توضیح. هم‌راستا با «هر صفحه با جواب شروع می‌شود، نه با داده».

---

## ۳′) یک موتور، سه نما — افشای لایه‌ای

همهٔ محاسبات در `lib/core/` (منبعِ واحدِ حقیقت). سه نما از **همان موتور** می‌خوانند — نه سه محصولِ جدا که نگهداری‌شان کابوس شود — فقط با **سطحِ افشای متفاوت**:

| نما | مخاطب | افشا | نمونه |
|---|---|---|---|
| **عمومی** | مردمِ متوسط | کیفی، جواب‌محور (لایهٔ ۱/۲) — همه‌جانبه و به‌روز | باندِ کیفی، روایتِ template، کارتِ نمادِ ساده |
| **ترمینالِ پرمیوم** | مشتریِ اشتراکی | عمیق، ساختارمند (لایهٔ ۲/۳) | کارت امتیازِ کامل، بک‌تست، تخصیص |
| **پنلِ رصدِ ادمین (آرش)** | فقط آرش | **کامل، خام، سورت/فیلترِ چندبعدی** | اسکرینرِ گلوگاه، همهٔ اعداد، همهٔ نمادها |

- **پنلِ ادمین = جایی که «آلفا» تولید می‌شود:** آرش با سورت/فیلترِ دقیقِ چندبعدی، **گلوگاه‌ها و فرصت‌های بازار** را رصد می‌کند؛ سپس بخشی از آن بینش (با کارنامه و به‌زبانِ ساده) به نمای عمومی می‌رسد. این، موتورِ محتوای ناحیهٔ ۳ (صدای مشاور) است.
- **سازگار با صداقتِ داده — و حتی تقویتش:** عددِ خامِ کاملِ چندبعدی فقط در پنلِ ادمینِ آرش (تحلیلگرِ مجوزدار)؛ نمای عمومی همیشه کیفی/دسترس‌پذیر. مرزِ «عددِ خام به عموم نه» این‌جا **معماری** می‌شود، نه صرفاً یک قاعدهٔ متنی.

---

## ۴) قاعدهٔ طلاییِ هر ابزار/فیچر جدید

قبل از ساخت، این‌ها را پاسخ بده — اگر گیر کردی، نساز:
1. **کدام نیاز (از ۷)؟ کدام ناحیه (از ۵)؟** اگر ذیلِ هیچ‌کدام نیست → ساخته نمی‌شود، حتی با دادهٔ مجانی.
2. **قیف را جلو می‌برد؟** جذب، اعتماد، یا تبدیل؟
3. **اصلِ دسترس‌پذیری را دارد؟** یک غیرحرفه‌ای هم می‌فهمدش؟
4. **داده در بودجهٔ سهمیه؟ محاسبه در `lib/core`+تست؟ نمایش با قانونِ طراحی + توکن/RTL/format-helpers؟** (قواعدِ اسکیل `iran-market-data`: فیلتر Z در مبدأ، append-only، null صادق.)

---

## ۵) مرزِ رایگان/پرمیوم (خودِ این مرز، ابزارِ بازاریابی است)

| رایگان (اعتماد‌ساز، SEO) | پرمیوم (پشتِ گیتِ entitlement) |
|---|---|
| نبض بازار · شناسنامهٔ نماد (سطحِ عمومی) · کارنامهٔ عمومی · فید کدال · یادگیری | ترمینال (کارت امتیاز کامل، بک‌تست، تخصیص) · دیده‌بانِ پیشرفته · هشدارها · مشاورهٔ آرش |

مرز باید در UI **صریح** باشد (چه چیز پشتِ گیت، چرا ارزشش را دارد)، نه ضمنی.

---

## ۶) متریکِ موفقیتِ قیف (بدونِ این، فاز ۲ قابل‌سنجش نیست)

**کم، صادق، خودمیزبان، بی‌طرفِ حریمِ خصوصی** (بدونِ اسکریپت/CDN خارجی):
1. بازدیدِ ارگانیکِ ناحیهٔ ۱/۲ (جذب).
2. نرخِ کلیک از عمومی به کارنامه (اعتماد).
3. ثبت‌نامِ وبینار (درگیرسازی).
4. تبدیل به ترمینال/مشاوره (درآمد).

متریک باید به «اعتماد»، نه «رشدِ انبوه به‌هر‌قیمت»، هدایت کند — سازگار با ماهیتِ پرمیوم.

---

## ۷) نقشهٔ فاز — «به کجا می‌رویم»

| فاز | تمرکز | تعریفِ «تمام‌شدن» |
|---|---|---|
| **۰ — انسجام** (اکنون) | مرتب‌کردنِ خانه | هیچ دو صفحه یک سؤال را دوبار جواب ندهد · ناوبریِ واحد بر پایهٔ ۵ ناحیه · این سند + اسنادِ هم‌راست · `build` سبز |
| **۱ — عمق + دسترس‌پذیری** | هر ناحیه از «هست» به «فهمیدنی» + نمای ادمین | لایهٔ ۱ (جوابِ یک‌خطی) + واژه‌نامهٔ درجا روی ناحیهٔ ۱/۲ · **کارتِ بنیادیِ نمادمحور (به‌سبکِ رباتِ کدال۷۲۴ — نیازِ بازِ فعلی)** · **پنلِ رصدِ ادمین (اسکرینرِ گلوگاه، سورت/فیلترِ چندبعدی — ابزارِ کارِ آرش)** · تب کندل/تکنیکال · رژیمِ بازارِ عمومی |
| **۲ — قیف + تبدیل + آموزش** | اتصالِ ۵ ناحیه | ناحیهٔ ۵ (یادگیری) ساخته شود · مرزِ صریحِ رایگان/پرمیوم · متریکِ قیف فعال · صفحهٔ «چرا ما» |
| **۳ — پوشش + مقیاس** | گستره | پوششِ کامل‌ترِ نمادها · SEO · اسکرینرِ بنیادی · پرتال API |

**قیدِ کارنامه (بحرانی):** کارنامهٔ عمومی فقط وقتی رو شود که **حداقل ۵–۱۰ رکوردِ واقعیِ باز/بسته** داشته باشد (از تحلیل‌های روزانهٔ آرش در تلگرام/اینستا — یک‌بار ثبت، هم آنجا هم اینجا با هش). **کارنامهٔ خالی بدتر از نبودنش است.**

---

## ۸) فاز ۰ به‌تفصیل — کارهای انسجام (ویرایشِ جراحی؛ منطقِ endpointها دست‌نخورده)

1. **ادغامِ داده/نماد:** `/data` (اکسپلورر) در `/market/stocks` ادغام و **۳۰۱ دائمی**؛ `history.csv` و لینک‌ها حفظ. **قبلش: اگر `/data` ورودیِ گوگل دارد، Search Console چک شود تا مقصد همان intent را بپوشاند.** (`/data/[symbol]` از قبل ریدایرکت است ✓.)
2. **ادغامِ محتوا:** `/insights` + `/notes` → یک ناحیهٔ «محتوا» با فیلترِ دسته؛ پنلِ ادمین یکپارچه. ذیلِ ناحیهٔ ۳/۵.
3. **ناوبریِ واحد بر پایهٔ ۵ ناحیه:** بازار · نماد · تحلیل‌ها/کارنامه · یادگیری · ترمینال (+ ورود/داشبورد). فوتر: حقوقی + درباره؛ بدونِ لینکِ مرده.
4. **هم‌راست‌کردنِ اسناد:** این Blueprint + به‌روزرسانیِ صادقانهٔ `DSS-STATE.md` (اعلامِ دو دوباره‌کاریِ باقی تا رفع) + یک‌خط ارجاع در سرِ `PRODUCT-MAP.md`.
5. **پاک‌سازیِ کم‌ریسک:** حذفِ Pricing/Plans قدیمی — **فقط با grep کاملِ importها + بیلدِ سبز**. حذفِ پرچمِ `TERMINAL_PREVIEW_OPEN` (گیت فقط DB-محور).

**قید:** append-only دست‌نخورده؛ هیچ UPDATE/DELETE مخرب؛ هیچ تغییرِ منطقِ فرم/پرداخت؛ فقط ساختار، ناوبری، ریدایرکت.

---

## ۹) نقشهٔ صفحاتِ فعلی، ذیلِ ۵ ناحیه

| ناحیه | صفحاتِ موجود |
|---|---|
| ۱ نبض بازار | `/` (بخش بازار) · `/market` · `/market/map` · `/market/stocks` · `/market/funds` · `/market/options` · `/codal` |
| ۲ شناسنامهٔ نماد | `/symbol/[symbol]` · `/data/[symbol]`→ریدایرکت · `/data`→(ادغام) · API `history.csv`، `symbol-detail` |
| ۳ صدای مشاور | `/analyses` (کارنامه) · `/insights`+`/notes`→«محتوا» · ادمین: analyses/notes/content |
| ۴ ترمینال حرفه‌ای | `/terminal` · `/terminal/[symbol]` · `/terminal/watchlist` · `/terminal/allocation` · `/dashboard` |
| ۵ یادگیری/سواد مالی | `/webinars` (+ ادمین) · **[جدید در فاز ۲: واژه‌نامه، آموزشِ گام‌به‌گام]** |
| زیرساخت (خارج از ۵ ناحیه) | auth · `/admin/*` · `/payment/*` · `/legal/*` · APIهای cron/telegram/payment/waitlist |

---

## ۱۰) اصولِ حاکم (ارجاع، نه تکرار)

- **صداقتِ داده:** بازه+فروض نه قیمتِ هدف · driver برای هر امتیاز · null نه عددِ ساختگی · qualitativeMask به LLM. (کاملِ قواعد: `TERMINAL-ANALYST-RULES.md` و اسکیل `iran-market-data`.)
- **واژگانِ مشاورِ دارای مجوز:** «سیگنال/خرید/فروش/توصیه/پیشنهاد» ممنوع (تضمین: `vocab.test.ts`).
- **یک موتور، سه نما:** همهٔ محاسبات در `lib/core/`؛ عمومی، ترمینالِ پرمیوم، و پنلِ رصدِ ادمینِ آرش از همان می‌خوانند با افشای لایه‌ای (بخش ۳′).
- **RTL/توکن/فونتِ سلف‌هاست/format-helpers** طبق `CLAUDE.md`.
