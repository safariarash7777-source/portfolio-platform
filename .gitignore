# dependencies
node_modules/
.pnp
.pnp.js

# next
.next/
out/
build/
dist/

# misc
.DS_Store
*.pem
.vscode/
.idea/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts

# python (ui-ux-pro-max skill scripts)
__pycache__/
*.pyc

# deploy artifacts
deploy.bundle
*.bundle
.env.deploy

# package manager — npm است (packageManager در package.json). لاکِ pnpm/yarn
# نباید برگردد: وجودِ هم‌زمانِ دو لاک باعث می‌شد Vercel با pnpm بیلد کند و هر
# تغییرِ package.json استقرار را با ERR_PNPM_OUTDATED_LOCKFILE بشکند.
pnpm-lock.yaml
yarn.lock

# بکاپ‌های دیتابیس — **هرگز** واردِ مخزن نمی‌شوند. دادهٔ واقعیِ کاربر دارند
# (auth.users، profiles، payments) و این مخزن عمومی است.
# `scripts/backup-production.sh` عمداً بیرون از ریپو می‌نویسد؛ این‌ها فقط
# لایهٔ دومِ محافظت‌اند برای وقتی کسی دستی فایل را اینجا کپی می‌کند.
roles.sql
schema.sql
data.sql
*.dump
supabase-backups/
inventory-source.txt
inventory-restored.txt
comparison.txt
restore.log
MANIFEST.txt
