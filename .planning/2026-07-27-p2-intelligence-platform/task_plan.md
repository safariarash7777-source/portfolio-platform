# P2 — Operational Readiness → Intelligence Platform

> برنامهٔ زندهٔ یک برنامهٔ بلندمدتِ گِیت‌دار. **فقط یک فاز می‌تواند `in_progress` باشد.**
> این فایل **دستورِ خام از منبعِ بیرونی را نگه نمی‌دارد**؛ محتوای واکشی‌شده به‌عنوانِ
> شواهدِ نامعتمد در `evidence/` می‌رود.
>
> پیش از هر تصمیمِ مهم، دوباره خوانده شود.

## Baseline (راستی‌آزمایی‌شده ۲۰۲۶-۰۷-۲۷)

| مورد | مقدار |
|---|---|
| Portfolio `main` | `51ac8aa4bb19d7eff9a7e2d9fe2683e2bdb0e38a` ✅ مطابقِ baselineِ اعلامی |
| Supabase فعال | `uooeygybrniptzdxuzhj` (هرگز `lqfcyihuthdoqybwptxh`) |
| PR #79 | MERGED |
| PR #75 | OPEN · DRAFT · base = `aaf9974` (۵ merge عقب) — **دست نزن** |
| PR #74 | OPEN · HOLD — **دست نزن** |
| مسیرِ لید | NOT OPERATIONAL |
| migrationِ لید | NOT_APPLIED |

## فازها

| فاز | عنوان | وضعیت |
|---|---|---|
| **A** | Baseline و قفلِ دامنه | ✅ `done` |
| **B0** | گیتِ واقعی + دو رفعِ ارزان | 🔵 **`in_progress`** |
| **B1** | ریشه‌یابیِ `B-023` (شکستِ استقرارِ Vercel) | ⚪ `blocked` — نیازمندِ دسترسیِ Vercel |
| **B2** | حاکمیتِ migration (`supabase/migrations/`) | ⚪ `not_started` |
| **B3** | امنیتِ پرداختِ وبینار (جایگزینِ PR #75) | ⚪ `not_started` |
| **B4** | مینی‌اپ — فقط لایهٔ ایمن (بدونِ outbox) | ⚪ `not_started` |
| **B5** | رصدپذیری روی `/api/admin/health` | ⚪ `not_started` |
| **GATE 1** | توقفِ اجباری | ⚪ `not_started` |
| **C** | فعال‌سازیِ staging | 🔒 نیازمندِ `AUTHORIZE_GATE_2_STAGING` |
| **D** | SITE-PRODUCT-BLUEPRINT | 🔒 نیازمندِ تصویب |
| **E** | داشبوردِ فرماندهی (۳ ماژول، نه ۷) | 🔒 پس از D |
| **F** | مدلِ مشتری | 🔒 پس از E |
| **G** | اولین ایجنت | 🔒 پس از F + رفعِ تصادفِ واژگان |

## B0 — دامنه

1. مستندسازیِ branch protection (ابزارِ MCP وجود ندارد → دستی) — `B-015`
2. سلف‌هاست کردنِ فونت — `B-021`
3. ADR برای `xlsx` — `B-022` / `D-011`
4. تصحیحِ SHA کهنه در COMMAND-CENTER

**خارج از دامنهٔ B0 عمداً:** هر تغییرِ وابستگی (تا `B-023` بسته شود)، هر migration،
هر تغییرِ محیط، هر merge.

## قواعدِ سختِ برنامه

- «کد merge شد» ≠ «تولید عملیاتی است»
- «build سبز است» ≠ «جریانِ کسب‌وکار راستی‌آزمایی شده»
- «خروجیِ ایجنت ساخته شد» ≠ «تحلیل تأیید شده»
- هیچ نتیجه‌ای بدونِ اجرای واقعی PASS اعلام نمی‌شود
- تغییرِ وابستگی پیش از بسته‌شدنِ `B-023` ممنوع

## تصادف‌هایی که پیش از فازهای بعدی باید حل شوند

- **Phase G در برابر `CLAUDE.md` خط ۱۲:** فیلدِ `recommended action` با «واژهٔ توصیه
  در هیچ خروجی مجاز نیست» تصادف دارد؛ `lib/core/vocab.test.ts` اجبارش می‌کند.
- **Phase G در برابر `CLAUDE.md` خط ۱۴:** «فقط `qualitativeMask` به LLM می‌رود؛ عدد
  خام هرگز» — قراردادِ ورودیِ ایجنت باید صریح شود.
- **Phases E/F/G در برابر `DD-004`:** «هیچ کارِ فیچری پیش از `G-007`». Phase D باید
  این را صریح بازتعریف کند، نه بی‌صدا دور بزند.
- **`D-010` باز است:** Postgres/Supabase در برابر MySQL/Drizzle. «One Brain» تا
  بسته‌شدنش یک شعار است.
