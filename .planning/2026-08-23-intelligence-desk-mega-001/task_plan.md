# P2-INTELLIGENCE-DESK-MEGA-001 — نقشهٔ اجرا

> مأموریت: تبدیل سطوح داخلیِ پراکنده به **یک سیستمِ عاملِ روزانهٔ شش‌سؤالی** روی
> `/admin/desk`. لایه، نه جایگزین. هیچ داشبوردِ سومی ساخته نمی‌شود.
>
> حاکمیت: `DD-024` (معماریِ تأییدشدهٔ میزِ آرش) · `PRODUCT-BLUEPRINT` §۹ و §۱۱ ·
> `DD-022` (بدونِ اثباتِ کادنسِ دستی، Agent Inbox شروع نمی‌شود).

## مرزها

| مجاز | ممنوع |
|---|---|
| ادامهٔ PR #115 (بدون PR جدید) | merge کردنِ #115 |
| rebase با `--force-with-lease` روی SHAی پین‌شده | تغییر Production / Supabase / migration / schema / secret / env |
| میزِ داخلی، ناوبری، view modelهای مشترک، تست، سند | دست‌زدن به #113، پرداخت، entitlement |
| حفظ موتورهای تخصصی به‌عنوان drill-down | تغییر صفحهٔ عمومی (کارِ #116) مگر تعارضِ اجتناب‌ناپذیرِ کامپایل |
| منطقِ ترکیبیِ خالص و تایپ‌دار روی موتورهای موجود | ساختِ شبکهٔ LLM/Agent |
| redirect و ادغامِ ناوبری با شواهد | جعلِ خبر، داده، وزن، اطمینان، تازگی یا سلامت |

## Waveها و گیت‌ها

| Wave | خروجی | گیت |
|---|---|---|
| 0 | rebase امنِ #115 روی main زنده | Draft، تمیز، بدون drift |
| 1 | فهرستِ کاملِ سطوح + ماتریسِ تصمیم | هر سطح نقشِ مستند دارد؛ هیچ کاری گم نمی‌شود |
| 2 | قراردادِ مشترکِ Desk + هفت حالتِ متعارف | دو مسیر نمی‌توانند دو حقیقتِ رقیب بسازند |
| 3 | پوسته و سلسله‌مراتبِ بصریِ شش‌ناحیه‌ای | کاربرِ تازه بدونِ مسیرِ دیگر رخداد/ریسک/تصمیم/خرابی را می‌بیند |
| 4 | Today و گردشِ رخدادِ مهم | «چه تغییر کرد و چرا» بدونِ نتیجه‌گیریِ بی‌پشتوانه |
| 5 | سناریو، سیگنال و حافظهٔ تحلیلی | ردِ تحلیل از شاهد تا تصمیم تا نتیجه |
| 6 | میزکارِ سبدِ مرجع ۷۰/۱۵/۱۵ (fail-closed) | بدونِ نقشهٔ ابزار، «پیکربندی نشده» |
| 7 | مشتری، محصول و صفِ تأیید | پیگیریِ انسانی بدونِ ساختِ CRMِ دوم |
| 8 | لایهٔ عملیات و اعتماد | هر منبعِ حیاتی می‌تواند آشکارا خراب شود |
| 9 | ناوبری و نامزدهای حذف | یک نقطهٔ شروع، بدونِ قابلیتِ گم‌شده |
| 10 | راستی‌آزماییِ رفتاری/بصری + شواهد | CI و Preview سبز روی headِ دقیق |

## قواعدِ سختِ محتوایی

- `null` صفر نیست. خطا دادهٔ خالی نیست. مهرِ زمانیِ سراسری برچسبِ فیدِ دیگر نمی‌شود.
- هیچ پنلی با جدولِ خام شروع نمی‌شود.
- سبدِ مرجع ۷۰/۱۵/۱۵ **قرارداد نسخه‌دار** است؛ ابزارِ تأییدنشده hardcode نمی‌شود.
- ژئوپلیتیک v1 دستی و منبع‌دار است؛ اسکرپ نمی‌شود.
- نتیجهٔ داخلی به‌صورت پیش‌فرض خصوصی است؛ انتشار تأییدِ انسانی می‌خواهد.
