# پیشرفت

| Wave | وضعیت | SHA | شواهد |
|---|---|---|---|
| 0 — baseline و rebase | ✅ انجام شد | `a1c71e9` → `236f1ef` روی `1b4ca75` | گیتِ کامل سبز؛ بدونِ drift |
| 1 — فهرست و ماتریس | ✅ انجام شد | `ec86f97` | `findings.md` + ماتریسِ تکمیلی در `INTELLIGENCE-DESK-RATIONALIZATION.md` |
| 2 — قراردادِ مشترک | ✅ انجام شد | — | ۷ حالتِ متعارف · ۶ ناحیه · `observedAt`/`fetchedAt` هر منبع · ۱۰ تستِ تازه |
| 3 — پوسته و سلسله‌مراتب | ✅ انجام شد | — | نوارِ تریاژ (۶ تست) · ناحیهٔ «مشتری و محصول» · ۶ لنگرِ ناحیه |
| 4 — Today | ✅ انجام شد | — | `event-inbox.ts` — حذفِ تکرار با contentHash، منبعِ تأییدنشده شمرده می‌شود، ژئوپلیتیکِ دستی · ۱۴ تست |
| 5 — سناریو و حافظه | ✅ انجام شد | — | `analysis-memory.ts` — ردِ شاهد→تصمیم→نتیجه، چهار حالتِ کارنامه، گیتِ کیفیتِ شاهد · ۱۵ تست |
| 6 — سبدِ مرجع | ✅ انجام شد | — | قراردادِ نسخه‌دار ۷۰/۱۵/۱۵ · fail-closed · ۱۳ تست · ناحیهٔ چهارم رندر شد |
| 7 — مشتری و صفِ تأیید | ✅ انجام شد | `2386f29` | `approval-queue.ts` — سه صفِ **فیلترشده** (`status`/`invite_sent`) نه شمارشِ کلِ جدول؛ صفِ ناخوانا هرگز «۰» نمی‌شود و سرخط «دستِ‌کم» می‌گوید؛ بدونِ دادهٔ شخصی · ۱۱ تست · ۲ گارد |
| 8 — عملیات و اعتماد | ✅ انجام شد | `ebedb32` | حالتِ هشتم `present` (ده منبعِ بی‌آستانه دیگر «به‌روز» نیستند) · `cron_runs` دو ردیف شد با فیلترِ `status = succeeded` · `clock.ts` مهرِ زمانیِ هر منبع را **رندر** می‌کند · `registry.test.ts` جلوی تستِ اجرانشده را می‌گیرد · ۴ گارد |
| 9 — ناوبری | ✅ انجام شد | — | `/admin` → `/admin/desk` · آمار به `/admin/overview` منتقل شد · ۶ تستِ ناوبری |
| 10 — راستی‌آزمایی | ✅ انجام شد | — | ۴ تصویر · ۳۶ بررسیِ رفتاری · **۲۱** گاردِ اثبات‌شده (۱۵ تا پیش از Wave 8، ‏۴ در Wave 8، ‏۲ در Wave 7) · `docs/assets/intelligence-desk-v2/` |

> ⚠️ تصحیحِ ادعای قبلیِ همین جدول: سطرِ پیشینِ Wave 8 نوشته بود «`DeskBoard`
> از قبل مهرِ زمانیِ هر منبع … دارد». **نادرست بود.** Wave 2 دو مهرِ زمانی به
> *قرارداد* افزود، ولی `DeskBoard` هیچ‌کدام را رندر نمی‌کرد؛ تنها زمانِ روی
> صفحه یک مهرِ سراسری در پایین بود. با `grep` روی `observedAt|fetchedAt|
> ageMinutes` در کامپوننت سنجیده شد: صفر مورد.
