# CLAUDE.md — پلتفرم آرش صفری

پلتفرم تحلیل ریسک و طراحی پرتفوی سرمایه‌گذاری برای بازار ایران. قوانین شخصی من در `~/.claude/CLAUDE.md` است؛ اینجا فقط چیزهای مخصوص این ریپو.

> **قبل از هر کاری، اول [`docs/README.md`](docs/README.md) را بخوان** — نقشهٔ اسناد.
> سه سؤال، سه مرجع: «چه می‌سازیم و چرا» → `docs/PRODUCT-BLUEPRINT.md` ·
> «الان کجاییم و چه گیر است» → `docs/COMMAND-CENTER.md` ·
> «چه تصمیمی گرفته‌ایم» → `docs/DECISION-LOG.md`.
>
> ⚠️ `docs/DSS-STATE.md` تا مرداد ۱۴۰۵ سندِ «کجاییم» بود؛ آن نقش به `COMMAND-CENTER`
> منتقل شده. محتوای فنی‌اش معتبر است، ادعای وضعیت و اولویتش نه.

> **قبل از هر کاری روی داده/رله/فید/نماد/صندوق/NAV/کدال/BrsApi، اول اسکیل `.claude/skills/iran-market-data/SKILL.md` را بخوان** — قواعد سخت زیرنمادها، NAV، سهمیه و نمادشناسی آنجاست.

## گزارش به آرش — قاعدهٔ دائمی
ایستگاه تحویل و پذیرش: [issue #145](https://github.com/safariarash7777-source/portfolio-platform/issues/145).
در شروع هر چت، آخرین نظرهای آن و وضعیت `main` خوانده شود؛ نتیجهٔ پایان هر نوبت در issue کاری مربوط ثبت شود. متن چت به‌تنهایی مرجع تحویل نیست.

آرش برنامه‌نویس نیست و انگلیسی فنی نمی‌داند. گزارش فارسی، کوتاه و با نتیجهٔ قابل لمس باشد؛ اصطلاح ضروری در همان جمله فارسی توضیح داده شود. **شمارهٔ PR/SHA و تعداد تست فقط پیوست فنی‌اند، نه تیتر وضعیت.**

قالب ثابت: «چه چیزی الان کار می‌کند / چه چیزی هنوز کار نمی‌کند / اقدام بعدی و مسئول / تنها تصمیم یا دسترسی ضروری آرش».

سند راهبردی موازی ساخته نشود.

## قطب‌نمای بازار (DSS) — قوانین سخت
- این محصول **سامانهٔ پشتیبان تصمیم** است، نه ربات معامله‌گر؛ لایهٔ اجرا ندارد.
- ارزش‌گذاری همیشه **بازه + فروض** (سه سناریو) — هرگز قیمت هدف واحد.
- واژه‌های «سیگنال/خرید/فروش/توصیه/پیشنهاد» در هیچ خروجی UI یا متن تولیدی مجاز نیست.
- هر امتیاز باید driver (چرا) داشته باشد؛ دادهٔ ناموجود = `null`، هرگز عدد ساختگی.
- فقط خروجی `qualitativeMask` (باند کیفی) به سمت LLM می‌رود؛ عدد خام مالی هرگز.
- جدول‌های `codal_reports` و `symbol_history` append-only — UPDATE/DELETE ممنوع.
- اصل «یک موتور، دو نما»: همهٔ محاسبات فقط در `lib/core/engine.ts`؛ ترمینال و صفحات عمومی هر دو از همان می‌خوانند.
- هر تابع هسته قبل از UI تست دارد: `npm run test:core` و `npm run test:calc` (node:test + tsx).

## استک
Next.js 15 (App Router) · React 18 · TypeScript · Tailwind 3 · Supabase (`@supabase/ssr`) · نمودار: `lightweight-charts` + `recharts` · آیکون: `lucide-react`. بدون ORM؛ دسترسی داده مستقیم با Supabase client.

## فارسی و RTL
- کل UI فارسی و راست‌به‌چپ است (`<html lang="fa" dir="rtl">`)؛ چیدمان RTL باید بی‌نقص بماند.
- اعداد/پول/تاریخ فقط با هلپرهای `lib/format.ts`: `toPersianDigits` برای نمایش، `toLatinDigits` **قبل از** اعتبارسنجی ورودی، `formatToman` (پول = عددِ صحیحِ تومان)، `formatJalali` (تاریخ جلالی). رقمِ فارسی یا تاریخ را دستی نساز — ناهماهنگیِ hydration و پارسِ اشتباه می‌آورد.

## رنگ و فونت — فقط توکن
- منبع رنگ: `tailwind.config.js` + متغیرهای `app/globals.css` (`--navy`, `--gold`, `--surface`, `--line`, …). هیچ رنگِ خارج از توکن ننویس. سایه: `shadow-institutional` / `--shadow*`.
- فونت Vazirmatn با `next/font` سلف‌هاست است. **فونت از CDN لود نکن.**

## داده — نمایش و ذخیره
- دادهٔ نمونه/دمو در UI باید برچسب صریحِ «نمونهٔ نمایشی» داشته باشد.
- ذخیره باید **append-only/نسخه‌دار** باشد؛ upsert مخربی که رکورد قبلیِ کاربر را نابود کند ممنوع.

## سه‌گانهٔ Supabase (اشتباه نگیر)
- `lib/supabase/client.ts` → کامپوننت کلاینت (مرورگر) · `server.ts` → RSC و route handler (با کوکی) · `admin.ts` → service-role، `server-only`، RLS را دور می‌زند؛ فقط verify پرداخت و وبهوک تلگرام.
- **`admin.ts` را هرگز در کامپوننت کلاینت import نکن** (کلید سرویس‌رول لو می‌رود).
- Auth در `middleware.ts`: مسیرهای `/dashboard` و `/admin` گیت می‌شوند؛ نقشِ admin از `profiles.role` در دیتابیس خوانده می‌شود (منبعِ واحدِ حقیقت).

## هنگام تغییر پوسته
منطقِ endpointهای فرم (مثل `/api/waitlist`) را عیناً حفظ کن؛ فقط ظاهر را عوض کن.

## دستورها و پذیرش
```bash
npm run dev        # توسعهٔ محلی
npm run build      # باید سبز باشد — پایان هر کار
npm run typecheck  # اگر تایپ‌ها را لمس کردی
npm run lint
```
متغیرهای محیطی در `.env` (نمونه: `.env.example`)؛ سکرت‌های service-role / زرین‌پال / تلگرام / cron فقط سمت سرور.
