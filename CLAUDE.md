# CLAUDE.md — راهنمای کار روی این ریپو

پلتفرم تحلیل و مشاورهٔ سرمایه‌گذاری **آرش صفری** (مشاور دارای مجوز رسمی).
این فایل قوانین لازم‌الاجرا برای هر سشن Claude Code روی این پروژه است.

## استک فنی
- **Next.js 15** (App Router) · **React 18** · **TypeScript**
- **Tailwind CSS 3** (توکن‌های برند در `tailwind.config.js`)
- **Supabase** (`@supabase/ssr` + `@supabase/supabase-js`) برای auth و دیتابیس
- آیکون‌ها: `lucide-react` · بدون Prisma و بدون ORM دیگر (دسترسی داده مستقیماً از طریق Supabase client)

## زبان و جهت
- کل رابط کاربری **فارسی و RTL** است (`<html lang="fa" dir="rtl">`).
- متن‌ها فارسی، اعداد فارسی؛ چیدمان راست‌به‌چپ باید بی‌نقص بماند.

## رنگ و فونت — فقط از توکن‌ها
- **هیچ رنگ یا فونتِ خارج از توکن‌ها مجاز نیست.** منبع رنگ: `tailwind.config.js`
  (navy / navy.deep / navy.soft · gold / gold.soft / gold.tint · bg · surface · ink · muted · line)
  و متغیرهای CSS معادل در `app/globals.css` (`--navy`, `--gold`, `--surface`, `--line`, …).
- سایه: `shadow-institutional` / متغیرهای `--shadow*`. گردی: `xl`/`2xl` یا `--r-*`.
- فونت: `--font-display` / `--font-body` (Vazirmatn، سلف‌هاست‌شده با `next/font`).
  **فونت از CDN لود نکن.** اگر فایل‌های فونت Pelak به ریپو اضافه شد، با `next/font/local` سلف‌هاست شود.

## داده‌ها و صداقت (مشاور دارای مجوز — ریسک قانونی)
- **هیچ عدد، آمار، درصد یا تستیمونیالِ ساختگی، مطلقاً ممنوع.** عدد فقط وقتی مجاز است که واقعی و قابل‌اثبات باشد.
- هیچ وعدهٔ سود یا تضمین بازدهی داده نشود.
- دادهٔ نمونه/دمو در UI باید صراحتاً برچسب «نمونهٔ نمایشی» داشته باشد.
- الگوی ذخیرهٔ داده باید **append-only/نسخه‌دار** باشد؛ upsert مخربی که رکورد قبلی کاربر را از بین ببرد ممنوع است.

## دامنهٔ تغییر
- **پیش‌فرض: ویرایش جراحی (surgical).** فقط همان چیزی که تسک خواسته را تغییر بده؛ به تم/چیدمان/منطقِ نامرتبط دست نزن.
- بازنویسیِ کامل فقط وقتی مجاز است که تسک **صریحاً** آن را بخواهد و فقط در همان محدوده.
- منطق فرم‌ها (مثل لیست انتظار: endpoint `/api/waitlist`) هنگام تغییر پوسته باید عیناً حفظ شود.

## گردش کار سشن
- **push / PR / deploy در سشن‌ها ممنوع است** مگر آرش صریحاً اجازه دهد. تغییرات را اعمال و در صورت نیاز به‌صورت local کامیت کن.
- **پایان هر کار: `npm run build` باید سبز باشد** (و در صورت لمس تایپ‌ها `npm run typecheck`).
- گزارش نهایی: فایل‌های تغییرکرده + خلاصهٔ diff + چک‌لیست پذیرش.

## دستورهای پرکاربرد
```bash
npm run dev        # توسعهٔ محلی
npm run build      # بیلد production (باید سبز باشد)
npm run typecheck  # بررسی تایپ‌ها
npm run lint       # لینت
```
