// ─────────────────────────────────────────────────────────────────────────
// پیکربندی مشاور هوش مصنوعی — معماری پلکانی Anthropic Claude
// این مقادیر «قفل‌شده» هستند؛ تغییر مدل‌ها باید آگاهانه باشد.
// ─────────────────────────────────────────────────────────────────────────

/**
 * معماری پلکانی مدل‌ها:
 *  - guard:    غربال/گاردریل سریع و کم‌هزینه
 *  - chat:     چت پیش‌فرضِ مشاور (تعادل سرعت/هوش)
 *  - analysis: تحلیل سنگین و چندمرحله‌ای
 */
export const MODELS = {
  chat: "claude-sonnet-4-6",
  guard: "claude-haiku-4-5",
  analysis: "claude-opus-4-8",
} as const;

export type ModelTier = keyof typeof MODELS;

/** سقف توکنِ خروجی برای چتِ استریمی. */
export const CHAT_MAX_TOKENS = 2048;

/** بیشینهٔ تعداد پیام‌های تاریخچه که به مدل ارسال می‌شود (پنجرهٔ مکالمه). */
export const MAX_HISTORY = 40;

/** سقف دورهای حلقهٔ tool-use تا جلوگیری از حلقهٔ بی‌پایان. */
export const MAX_TOOL_ROUNDS = 6;
